
---

현재 저장소의 수집 파이프라인 안정성을 높이기 위해 아래 3가지 작업을 수행해줘. todo.md 파일 내용을 참고하여 순서대로 진행해 줘.

1. 크론 스케줄 시간대 KST 새벽 3시 기준으로 재조정
- ingest-daily.yml, ingest-monthly.yml 등 크론 워크플로 파일들의 schedule 설정을 수정할 것.
- GitHub Actions 기본 기준시는 UTC이므로, 한국 시간(KST) 기준 새벽 3시 대에 실행되도록 UTC 기준 전날 저녁 18시 대로 맞출 것 (예: KST 새벽 3시 23분 -> cron: '23 18 * * *').
- 전 세계 리포지토리와의 정각(Top of the hour) 트래픽 부하(Dropped Runs)를 피하기 위해 반드시 정각(0분)이 아닌 비정규 분 단위로 설정할 것.
- YAML 파일 내에 "KST 기준 새벽 3시 XX분 (UTC 18:XX)" 형태의 주석을 반드시 명시할 것.

2. 수집 파이프라인 자동 재시도(Retry) 메커니즘 도입
- API 호출 일시적 타임아웃, 네트워크 불안정, DB 부하 등으로 인해 수집/적재가 실패했을 때, 단 한 번의 실패로 당일 데이터가 유실되지 않도록 코드 레벨(어댑터 및 쿼리 업서트 부)에 재시도(Retry) 래퍼 함수를 적용할 것.
- 타임아웃이나 일시적 에러 발생 시 지정된 횟수(예: 최대 3회)와 간격(예: 3~5초)을 두고 자동으로 재시도하도록 구현할 것.

3. open_spaces 테이블 성능 최적화 및 검증 준비
- 프로덕션 DB 측에서 ANALYZE public.open_spaces; 처리는 완료된 상태이므로, 다음 수집 파이프라인 트리거 시 open_spaces 타임아웃이 깔끔하게 해소되는지 로그를 통해 확인할 수 있도록 코드를 점검할 것.
- 반복 재현 시 대비하여 배치 묶음 크기(Batch Size)를 쪼개어 처리하는 청크(Chunk) 단위 업서트 구조도 검토해 줄 것.

---
현재 open_spaces 테이블을 확인해보니, 위경도 좌표가 완전히 동일하거나 동일한 장소가 2개 이상 중복으로 쌓여 있는 데이터 이슈가 확인되었어. 기존의 '일단 유연하게 적재한다'는 수집 원칙은 유지하되, 아래 내용을 반영하여 중복 데이터 문제를 해결해 줘.

1. open_spaces 테이블 중복 데이터 식별 및 정제
- open_spaces 테이블 내에서 위경도(Latitude, Longitude)가 완전히 동일하거나, 이름/주소가 겹쳐서 중복으로 쌓여 있는 데이터를 찾아내고 정리(Deduplication)할 수 있는 스크립트 또는 쿼리를 작성해 줘.
- 앞으로 수집 과정에서도 동일한 중복 데이터가 계속 파고들지 않도록 업서트(Upsert) 기준 키나 고유 식별 로직을 보완할 것.

2. 유연한 적재 원칙 유지
- 단, 데이터가 일부 누락(예: 위경도나 주소가 비어있는 경우)되었더라도 버리지 않고 일단 적재하는 기존의 유연한 정책은 절대 훼손하지 말 것.
- 정제 작업은 오직 '명백하게 겹치는 중복 데이터'를 깔끔하게 잡아내는 데에만 집중할 것.

[open_spaces 카테고리 재구조화 지침]

대분류 2개, 중분류 7개 구조로 open_spaces 분류 체계를 개정하고 venue_name 기반 상속 로직에 반영하라.

---

현재 open_spaces 테이블의 수집 파이프라인 고도화와 데이터 정제 구조를 완성하기 위해, 아래 내용들을 순서대로 구현해 줘. todo.md에 반영하고 차근차근 진행해 줄 것.

1. 크론 스케줄 시간대 KST 새벽 3시 기준으로 재조정
- ingest-daily.yml, ingest-monthly.yml 등 크론 워크플로 파일들의 schedule 설정을 KST 기준 새벽 3시 대(UTC 기준 전날 저녁 18시 대)로 변경할 것.
- 정각(0분) 부하를 피하기 위해 비정규 분 단위로 설정하고, 주석으로 KST/UTC 기준 명시.

2. 수집 파이프라인 자동 재시도(Retry) 메커니즘 도입
- events 및 open_spaces 수집/적재 시 일시적 타임아웃이나 네트워크 에러 발생 시 최대 3회까지 자동 재시도(Retry)하는 래퍼 함수 적용.

3. open_spaces 테이블 중복 데이터 식별 및 정제 로직 구현
- 위경도가 완전히 동일하거나 이름/주소가 겹쳐서 중복으로 쌓이는 데이터를 식별하고 정리하는 로직 추가 (기존 유연한 적재 원칙은 유지).

4. open_spaces 세부 중분류 카테고리 자동 매핑 및 '기타' 예외 처리 로직 구축
- 공공 API 원본 카테고리 명칭들을 분석하여 아래 세부 중분류 체계에 맞게 자동으로 맵핑되도록 로직 구현할 것.
- 명확히 매핑되지 않거나 애매한 카테고리 데이터는 유실시키지 말고 **'기타'** 카테고리로 안전하게 적재하여, 추후 수동으로 분류를 보정할 수 있도록 구조화할 것.
  - [세부 중분류 목록]
    1. 역사박물관
    2. 종합/기타박물관
    3. 미술관
    4. 도서관 (독서실 제외)
    5. 공연장
    6. 전시장
    7. 문화원
    8. 문화의집 / 주민문화공간
    9. 체육관 / 실내체육시설
    10. 운동장 / 야외체육시설
    11. 근린공원 / 도심공원
    12. 생태공원 / 습지
    13. 수목원 / 식물원
    14. 자연휴양림 / 산림욕장
    15. 역사유적지 / 고궁
    16. 관광명소 / 테마파크
    17. 과학관 / 천문대
    18. 체험학습장 / 농어촌체험관
    19. 시민교육센터 / 평생학습관
    20. (필요시 추가)
    21. 기타 (매핑되지 않는 모든 예외 데이터 수용)
---

open_spaces 중분류 카테고리 자동 매핑 로직을 구현하기 전에, 데이터 정합성을 확보하기 위해 먼저 '매핑 시뮬레이션 및 키워드 검증' 단계를 진행하려고 해. todo.md에 반영하고 아래 순서대로 실행해 줘.

1. 원본 데이터 카테고리 분석 및 키워드 정의
- 현재 open_spaces 테이블(또는 수집 원본)에 들어있는 원본 카테고리 명칭과 이름(name)들의 분포를 먼저 분석할 것.
- 우리가 정한 16개 노출 대상 중분류(박물관, 미술관, 도서관, 공원 등)를 매핑하기 위한 핵심 키워드(정규식/포함 조건) 규칙 초안을 작성할 것.

2. 매핑 시뮬레이션 및 건수(Count) 통계 산출
- 위에서 정의한 키워드 규칙을 적용했을 때, 현재 DB에 있는 전체 데이터가 각 중분류별로 몇 건씩 매핑되는지 그 결과를 보여주는 **시뮬레이션 스크립트(또는 분석 쿼리)**를 작성 및 실행할 것.
- 어느 중분류에도 속하지 못하고 누락되거나 쏠리는 데이터(예: 예외 케이스)가 몇 건이나 되는지 통계로 리포트해 줄 것.

3. 결과 검토 후 매핑 룰 확정
- 시뮬레이션 결과를 보고, 키워드 누락이나 오매핑(Mis-matching)된 케이스가 있다면 키워드 규칙을 보완한 뒤 최종 매핑 로직을 코드에 반영할 것.

---

### 🔍 2. 추가 중분류 후보 탐색 및 발굴 요청 (Read-Only DB Analysis)

- 위 5개 중분류 조건에 매핑되지 않고 `category_min IS NULL`로 남아있는 open_spaces 야외/자연 레코드들의 `title`, `venue_name`, `parkSe` 패턴을 전수 조사하라.
- 특히 아래와 같은 키즈/가족 특화 야외 공간 시설이 원천 데이터에 존재하는지 확인하고, 필요시 신규 중분류(예: 캠핑/휴양, 농장/체험장, 유원지/야외놀이 등) 추천안을 작성하여 보고하라.

---
[open_spaces / events] NATIONAL_PARK_ECOTOUR 출처 데이터 일괄 비활성화 요청

NATIONAL_PARK_ECOTOUR(국립공원 생태탐방) 출처 데이터는 운영 여부 미확인 및 과거 만료 데이터 섞임으로 인해 서비스 데이터 품질을 저하시키므로 아래와 같이 조치하라.

---


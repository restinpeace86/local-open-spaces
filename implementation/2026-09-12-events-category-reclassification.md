# seoul_public_reservation 원천 대/중분류 재분류 (공간시설·체육시설 → 표준 대분류) + 어드민 필터 그룹핑 반영 (Step 126)

## 구현 일시
2026-09-12

## 배경 (사용자 지시 원문, 시간순)
1. "seoul_public_reservation의 원천 대/중분류가 공간시설인거.. 공간시설 / 다목적실이나
   회의실, 주민공유공간, 강의실등 시설대관성격인게 지금 표준 중분류가 룰에 의해서
   잘못 매핑돼서 표준 중분류 여기저기로 흩어져 있어.. 공공청사/행정 대분류 내에
   강의실이면 강의실 중분류로.. 시설대관성 항목은 다 그쪽으로 옮겨줘.. 교육강좌 /
   역사로 된 원천 대/중분류도 지금 문화/축제 >> 역사 중분류에 있는데 이거 배움/교육
   >> 역사로 넘겨줘"
2. "문화/축제 >> 전시실 항목 있는데 이거 원천 대 / 중분류가 공간시설/ 전시실인데?
   이것도 공공청사/행정쪽으로 옮겨줘"
3. "기타>> 수영장하고 기타 >> 파크골프장은 체육시설쪽으로 옮겨줘.. 해당 중분류
   2개는 체육시설 대관이야"
4. "어 그렇게 해.. 지금 다 옮겨야 할것같아" (전체 스캔 후 남은 것들도 일괄 처리 승인)
5. "내가 관리자의 events 탭보고 확인중인데 여기의 표준 중분류 조건은.. 전혀
   반영안됐어.. 대분류 문화/축제>>중분류 여전히 나오고 데이터 13건도 여기에
   들어있는걸로 나와 공간시설/전시실이 원천 대/중분류인거..."
6. "문화/축제 >> 공연장도 공간시설 대관이야.. 이것도 공공청사/행정의 공연장으로
   옮겨줘.. DB는 물론 관리자 화면도 그렇게 보이도록"
7. "자연/체험 대분류에 있는 광장 중분류에 대하여 공공청사/행정 대분류로 옮겨줘"
8. "배움/교육 대분류에 있는 교육체험에 대하여 대분류 자연/체험쪽으로 옮겨줘"

## 조사 결과
- `events.category_maj`는 두 가지 서로 독립된 경로로 관리된다:
  1. **데이터(DB 컬럼)**: 어댑터가 채우거나(`buildEventRow`), 배치
     재분류 엔진(`scripts/ingest/lib/category-maj-taxonomy.mjs`의
     `applyCategoryMajTaxonomy`)이 `category_min_source`(RAW/RULE/MANUAL)별로
     다르게 재계산한다. **MANUAL은 절대 건드리지 않는다** — 이번 모든 수정에
     `category_min_source='MANUAL'`을 써서 향후 이 엔진이 다시 돌아도 되돌아가지
     않게 보호했다.
  2. **어드민 필터 UI 그룹핑(`src/lib/admin/category-min-groups.ts`)**: `/admin/
     data-grid` events 탭의 "표준 중분류" 체크박스를 대분류별로 접어서 보여주기
     위한 **완전히 별개의 정적(hardcoded) 매핑**이다. DB의 실제 `category_maj`
     컬럼 값을 전혀 읽지 않고, `category_min` 문자열이 어느 정적 그룹의 `minors`
     배열에 나열돼 있는지로만 어느 대분류 아래에 보여줄지 정한다 — 그래서 DB
     데이터를 아무리 고쳐도 이 화면의 그룹핑은 안 바뀐다(항목 5의 실측 확인 내용).
- "역사"/"전시실"/"공연장" 같은 category_min 값은 **소스에 따라 성격이 완전히
  다르다** — 예: "역사" 2,607건 중 2,506건(96%)은 문화행사 API의 실제 전시/유적
  탐방 콘텐츠고, 78건(교육강좌 소스)만 진짜 강좌다. "전시실"은 16건(공간시설
  대관)과 19건(실제 문화체험/전시)이 거의 반반, "공연장"은 45건(공간시설 대관)이
  4건(실제 공연)보다 훨씬 많다. 하나의 category_min 값을 통째로 다른 대분류로
  옮기면 성격이 다른 나머지가 잘못 딸려가므로, **원천 MAXCLASSNM(공간시설/체육시설/
  교육강좌 등)으로 정확히 조건을 좁혀서만** 데이터를 수정했다.

## 변경 사항

### 1. 데이터 재분류(직접 SQL, 프로덕션 즉시 적용— 코드 변경 아님)
모두 `source='seoul_public_reservation'`, 조건에 맞는 행만, `category_min_source='MANUAL'`로 보호:

| 조건(원천 MAXCLASSNM/MINCLASSNM) | 결과 | 건수 |
| --- | --- | --- |
| 공간시설 / 다목적실·회의실·주민공유공간·강의실 | category_maj='공공청사/행정' | 593건 |
| 공간시설 / 전시실 | category_maj='공공청사/행정' | 16건 |
| 교육강좌 / 역사 (category_min='역사'는 유지) | category_maj='배움 / 클래스' | 78건 |
| category_min in (수영장, 파크골프장) ∧ 체육시설 | category_maj='체육시설' | 5건 |
| 공간시설 / (나머지 전부: 강당·공연장·광장·녹화장소·민원 등 기타·청년공간) | category_maj='공공청사/행정' | 나머지분 |
| 공간시설 / 캠핑장 | category_maj='자연 / 캠핑' (예외 — 레저 시설이라 행정 시설 대관이 아님) | 99건 |
| 체육시설 / (전체 17개 중분류) | category_maj='체육시설' | 1,407건 |

최종 재확인: `공간시설`·`체육시설` 두 원천 대분류 범위 전체에서 `category_min ≠
원천 MINCLASSNM` 또는 `category_maj` 불일치 **0건**.

### 2. 어드민 필터 그룹핑 코드 수정 (실제 코드 변경, 검증 필요)
`src/lib/admin/category-min-groups.ts`의 `EVENTS_GROUPS_STATIC`: `'전시실'`, 이어서
`'공연장'`을 `'문화/축제'` 그룹의 minors에서 제거하고 `'공공청사/행정'` 그룹의
minors로 이동. `src/lib/admin/category-min-groups.test.ts`: 신규 테스트 2개 —
"전시실"/"공연장" 각각 공공청사/행정 그룹에 배정되고 문화/축제 그룹에는 더 이상
없는지 확인.

**"공연장" 이동 근거**: DB를 재확인한 결과 category_min='공연장' 45건(공간시설
소스)이 이미 앞선 종합 재분류에서 '공공청사/행정'으로 반영돼 있었다(문화행사 API
등에서 온 4건만 실제 공연 성격이라 그대로 둠, 45:4로 시설대관이 압도적 다수) —
사용자가 이번에 명시적으로 이동을 확인해줘 그룹핑도 맞췄다.

**"광장" 이동 근거**: DB를 재확인한 결과 category_min='광장' 5건(공간시설 소스)이
이미 '공공청사/행정'으로 반영돼 있었다(문화행사/축제성 3건만 그대로 둠, 5:3) —
동일 패턴으로 어드민 그룹핑에서 '자연/체험' → '공공청사/행정'으로 이동.

**"교육체험" 이동은 성격이 다르다**: 앞선 4건(전시실/공연장/광장/전시실)은 모두
"같은 category_min 값이 소스에 따라 성격이 갈려서, 이미 DB는 맞게 갈라져 있는데
어드민 그룹핑만 안 맞았던" 경우였다. "교육체험"은 다르다 — 원천(문화체험 MAXCLASSNM
등)에 걸친 성격 혼재 문제가 아니라, "교육체험" 자체를 관리자가 "학습(배움/교육)"
보다 "체험(자연/체험)" 성격으로 재분류하기로 한 순수한 taxonomy 판단이다. 그래서
DB의 category_maj(현재 NULL 또는 '배움 / 클래스')나 이벤트픽 공개 화면의 taxonomy
(`category-maj-meta.ts`/`category-maj-taxonomy.mjs`)는 건드리지 않고, **어드민
필터 그룹핑만** 이동했다 — DB/공개 화면까지 함께 바꾸는 건 이벤트픽 홈 화면에
노출되는 카테고리 자체를 바꾸는 더 큰 결정이라 별도 확인 없이 임의로 확장하지
않았다(제3장 제5조 추측 금지).

**의도적으로 그대로 둔 것**: "역사"는 이 그룹핑 파일에서 옮기지 않았다 — 대다수
(2,506/2,607건, 96%)가 여전히 "문화/축제" 성격이라(공연장과 반대로 문화 콘텐츠가
압도적 다수), 그룹 자체를 옮기면 그 다수가 잘못된 그룹 아래 나열된다.

## 검증
- `npx tsc --noEmit`: 통과.
- `npm run test -- --run`: 135 files / 1585 tests 전체 통과(신규 4개 —
  category-min-groups.test.ts). `spot-category-groups.test.ts`(open_spaces 그룹
  교차검증, 이번엔 손대지 않은 `OPEN_SPACES_GROUPS_STATIC` 대상)도 회귀 없음 재확인.
- `npm run build`: 성공.
- 데이터 재분류는 SQL로 직접 실행 후 매 단계 재확인 쿼리로 실측 검증(위 표의
  건수는 모두 실제 UPDATE...RETURNING/재조회 결과).

## 특이 사항
- `문화체험`/`교육강좌` 원천 대분류의 나머지(역사 제외) 항목들은 이번 범위에
  포함하지 않았다 — 사용자가 확인 질문을 남긴 상태(마지막 답변에서 "그쪽도
  훑어봐 드릴까요?" 제안, 아직 승인 대기).
- 이 두 재분류 메커니즘(DB 컬럼 vs 어드민 UI 정적 그룹핑)이 서로 완전히 독립적이라는
  사실 자체가 이번 조사에서 새로 드러난 구조적 특징이다 — 앞으로 category_maj를
  데이터로 고칠 때마다 "어드민 화면에 보이는 그룹이 실제로 반영되길 원하면"
  `category-min-groups.ts`도 함께 확인해야 한다(자동 동기화되지 않음). "역사"/
  "공연장"처럼 소스별로 성격이 갈리는 category_min은 그룹 이동 전 실측 비율
  확인이 필수다(다수 쪽 성격을 따라야 함).

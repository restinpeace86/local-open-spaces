## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---
# To-Do List

[개선사항 1] 관리자 워크벤치 내에 [키즈카페 / 실내놀이터] 카테고리를 위한 스팟 큐레이션 팝업 UI 및 로직을 구현해 주세요. 기존 '키즈친화 식당' 팝업의 컴포넌트 구조를 베이스로 재사용하되, 다음 요구사항을 반영해 주세요.
'키즈카페 / 실내놀이터' 팝업 UI 구축
방금 정리한 세미오토 뱃지 검수(Opt-out), 가격 스마트 파싱, 옵셔널 메뉴가 들어가는 검수 팝업 적용
목적: 실내 키즈카페와 공공 실내놀이터 데이터를 빠르고 정확하게 정제하기 위함.

- [x] 1. 뱃지 세미오토 검수 (Opt-out) 구조
   - [x] 블로그 본문 및 키워드 데이터를 기반으로 시스템이 주요 뱃지(예: 주차 완비, 놀이방, 아기의자 등)를 1차로 자동 체크(Pre-check)해 주어야 합니다. — 2026-09-07 개선사항3-4로 이미 구현 완료(`matchBadgeKeysFromText`).
   - [x] 관리자는 이를 눈으로 검수하고, 문맥상 맞지 않는 항목은 클릭하여 간편하게 체크를 해제(Opt-out)할 수 있어야 합니다. — 위와 동일(`toggleBadge`).
   - [x] 트램폴린에 대하여 키워드 트램펄린도 가져가도록 할것. — [[2026-09-08-trampoline-synonym]] `curation-badges.ts`의 `kc_trampoline` 키워드에 '트램펄린' 추가.

- [x] 2. 팝업 기본 입력 필드 (식당 팝업 구조 재사용) — [[2026-09-08-kids-cafe-details-form]]
   - [x] 대표 이미지 등록 — SpotCurationsPanel의 클립보드 붙여넣기 업로드 UX 재사용.
   - [x] 영업시간 및 휴무일 정보 — operating_hours_raw(원문, 휴무일 문구 포함 가능) +
     구조화 필드(open/close/break/last_order) 자동 파싱. 별도 구조화된 "휴무일"
     컬럼은 기존 스키마에 없어 신설하지 않고, 원문 텍스트로 자유 기재하는 기존
     방식을 그대로 재사용했다(제5장 제4조 — "식당 팝업 구조 재사용" 지시와 일치).
   - **[2026-09-08 후속 수정]** [[2026-09-08-unmerge-blog-and-spot-curation]]:
     "블로그 뱃지큐레이션하고 스팟큐레이션 합쳤는데.. 다시 분리해줘.. 괜히
     길어져" 지시로 이 입력 필드들을 블로그 뱃지 워크벤치에서 다시 빼내
     원래 자리인 `SpotCurationsPanel`('스팟 큐레이션' 탭)로 되돌렸다.

- [x] 3. 가격 및 입장료 스마트 파싱 기능 — [[2026-09-08-kids-cafe-details-form]]
   - [x] 네이버 플레이스 등의 가격 텍스트를 그대로 복사·붙여넣기(Paste)할 수 있는 [가격 스마트 입력창]을 제공해 주세요.
   - [x] 내부 파싱 로직(정규식 등)을 통해 어린이 요금, 보호자 요금 등의 필드에 숫자가 자동으로 쪼개져 매핑되도록 하고, 관리자가 수정·저장할 수 있어야 합니다. — spot_curations에 child_fee/guardian_fee 컬럼 신설(migration), parseEntranceFeeText() 신규 파서. 위와 동일하게 최종 위치는 SpotCurationsPanel.

- [x] 4. 식음료 및 메뉴 정보 (옵셔널 처리) — [[2026-09-08-kids-cafe-details-form]]
   - [x] 키즈카페 내 판매하는 음료/간식 정보를 적는 메뉴 입력란을 두되, 필수가 아닌 [옵셔널(선택 입력)]로 처리하여 빈칸이어도 에러 없이 저장되도록 구현해 주세요. — 기존 menu_items(jsonb) 재사용, 빈 배열 기본값(SpotCurationsPanel, 원래부터 있던 위치라 변경 없음).

[개선사항 2] 관리자 워크벤치의 뱃지 자동 체크(Pre-check) 및 데이터 연동 로직을 다음과 같이 고도화해 주세요.

- [x] 1. 뱃지 상태별 시각적 색상 구분 (UI/UX) — [[2026-09-08-badge-color-state]]
   - [x] AI 시스템이 키워드 기반으로 1차 자동 체크를 수행했으나 아직 DB에 저장되지 않은 상태: **초록색 (Green)** 뱃지로 표시
   - [x] 기존에 이미 DB에 저장되어 불러와진 상태: **파란색 (Blue)** 뱃지로 표시
   - [x] (기존의 단순 검은색 표시를 위 상태에 맞게 변경) — `bg-gray-900`(검정) 전부 제거, 미체크는 기존 흰색/회색 그대로.

- [x] 3. 멀티 블로그 키워드 종합 분석 및 워닝(Warning) 필터링 — [[2026-09-08-multi-blog-badge-aggregation]]
   - [x] 현재 블로그 1의 키워드만 참고하는 구조를 수정하여, **블로그 1, 2, 3 등 연결된 전체 블로그의 키워드 및 본문**을 종합하여 뱃지를 자동 체크하도록 확장해 주세요.
   - [x] 단, **워닝(Warning)이 걸린 블로그는 키워드 분석 대상에서 제외(Drop)**해야 합니다.
   - [x] *워닝 판정 기준:* 탭 제목에 워닝 아이콘이 부착되어 있거나, 제목/본문 내에서 필수 지역 키워드를 찾지 못한 블로그 등은 신뢰도가 낮으므로 뱃지 자동 체크 계산에서 제외하고, 오직 정상 상태인 블로그들의 데이터만 취합하여 1차 뱃지를 체크해 주세요.

[개선사항 3] 유저 서비스 (스팟픽) UX/UI 구현

- [x] 1. **지도 및 정렬 (Wide-view & Distance Sort)** — [[2026-09-08-global-exposure-and-service-category-filter]]
  **[2026-09-08 재승인]** 아래 두 항목은 원래 같은 날 spec/데이터 정합성 충돌로
  스킵(보류)했었으나, 사용자가 두 트레이드오프(반경 컷오프 폐지, 노출 중분류
  커버리지 낮음)를 직접 확인한 뒤 명시적으로 재지시해 Decision 022로 승인·구현
  완료했다. `spec/map/spatial-search.md` §2.1/§3.1도 이 Decision에 맞춰 개정.
   - [x] 중분류는 우리가 정한 노출 대분류와 노출 중분류로 합니다. 현재 대분류 중분류는 다 걷어내고 노출 중분류 기준으로 보여줍니다. — service_categories(대분류=parent_category/중분류=category_name) 기준으로 전면 교체.
   - [x] 특정 반경(예: 3km 등)으로 핀을 제한하는 컷오프를 없애고, 선택된 중분류에 해당하는 서울/경기 전역의 핀을 지도에 넓게 노출합니다. — 중분류 선택 시 신규 RPC(get_spots_by_service_category)로 반경 무관 전국 조회, 지도 스케일별 클러스터링은 기존 그대로.
   - [x] 하단 바텀시트 리스트는 유저의 **'현재 GPS 위치'를 기준으로 가까운 거리순**으로 정렬합니다. (단 하단 바텀시트 리스트는 현재 설정한 위치 기준 반경 10km 로 제한합니다.) — [[2026-09-08-gps-sync-and-sheet-sort]]로 10km 고정 구현 후, 이번에 5/10/20km 중 사용자가 눌러서 고를 수 있게 확장(지도 전역 노출과는 무관하게 바텀시트에만 적용).
- [x] 3. **실시간 위치 싱크 (GPS Sync 팝업)** — [[2026-09-08-gps-sync-and-sheet-sort]]
   - [x] 유저가 사전에 설정한 동네와 현재 실제 GPS 위치 간에 유의미한 차이가 감지될 경우, *"현재 위치({동네 이름})로 위치를 변경할까요?"* 형태의 팝업을 띄우고, 수락 시 기본 위치 설정을 현재 GPS 기준으로 업데이트합니다. — "유의미한 차이"의 구체적 임계값이 지시문에 없어 보수적 기본값(2km) 채택, 근거를 코드 주석에 명시.
- [x] 4. **마커 및 바텀시트 인터랙션 (Preview ➡️ Detail Flow)** — [[2026-09-08-price-menu-placeholders]]
  2026-09-08 확인: 2단계 플로우 자체(마커→프리뷰→상세)는 2026-09-01 "스팟픽
  UI/UX 개선 4종" 항목1로 이미 구현 완료돼 있었으나, 프리뷰 카드 내용물이
  요구사항(대표 이미지/핵심 뱃지)에 못 미쳐(예전엔 카테고리 색상 아이콘만)
  이번에 보강했다.
   - [x] **1단계 (프리뷰):** 지도 핀 또는 바텀시트 리스트를 누르면, 지도가 해당 위치로 이동하며 하단에 **[요약 프리뷰 카드]** (대표 이미지, 이름, 핵심 뱃지)가 팝업됩니다. — 대표 이미지/핵심 뱃지(최대 2개)를 /api/spot-curations에서 가볍게 조회해 추가.
   - [x] **2단계 (상세 뷰):** 프리뷰 카드를 터치하면 전체 화면의 **[상세 페이지]**로 확장 진입합니다. 여기에 영업시간 / 가격 / 메뉴 등의 정보들이 들어있음. — 기존 구현 그대로(변경 없음).
- [x] 5. **가격 및 메뉴 '준비 중' 플레이스홀더 처리** — [[2026-09-08-price-menu-placeholders]]
   - [x] MVP 단계에서 가격이나 메뉴 데이터가 아직 채워지지 않은 스팟의 경우, 빈 화면 대신 아래와 같은 플레이스홀더 문구를 노출하여 신뢰도를 유지합니다.
     - 가격 영역: *"가격 정보 업데이트 준비 중입니다 ⏳"* (단, 공공데이터로 이미 무료임을 아는 경우 "무료입장"으로 정확히 표시)
     - 메뉴 영역: *"상세 메뉴 정보는 순차적으로 추가될 예정이에요"*
     - 영업시간 영역: *"영업시간 정보는 순차적으로 추가될 예정이에요"*

[동일 장소 중복 스팟 처리] "8월 일반캠핑존 C형.. 26년 한강공원 난지캠핑장 이렇게
되어있는것들은 예약기준으로는 별도로 가는게 맞는데 장소기준으로는 난지캠핑장
하나 아니야?" → (실측 확인 후 사용자 확인) "장소 단위로 묶어서 대표 1건만 노출
하는 걸로 하자.. 다건에 대하여서는 클릭시 쫙 뜨는걸로 하자.. 중복스팟 검수 및
매핑 있는데 이 탭에 있는 것을 스팟 큐레이션 옮긴 것처럼 해당 스팟 상세에 대하여
버튼 만들어서 중복 스팟 검색이라던가 검수라던가 매핑이라던가 해야하나?"

- [x] 1. open_spaces 상세에서 곧장 시작하는 중복 스팟 검토(관리자, 다건 동시 병합) — [[2026-09-09-spot-dedup-quick-review-from-open-spaces-detail]]
   - [x] `find_nearby_open_spaces` RPC 기본 limit 5→50 상향(N-way 중복 대응, 특정 카테고리 하드코딩 없음).
   - [x] `SpotDedupQuickModal` 신규: 30m 반경 후보를 체크박스로 여러 건 한 번에 선택해 기존 `GroupDetailModal`/`/apply`로 한 번에 병합.
   - [x] `raw-data-modal.tsx` open_spaces 상세에 "🔗 중복 스팟 검토" 버튼 추가.
- [x] 2. 장소 단위 대표 1건 노출 + 그룹 펼쳐보기(소비자 화면) — [[2026-09-09-group-representative-exposure]]
   - [x] `get_nearby_spaces_and_events`/`get_spots_by_service_category`에 `group_id` 추가 후 그룹당 대표 1건만 반환(대표 기준: 반경 조회는 최근접, 전국 조회는 최초 적재).
   - [x] 신규 `get_spot_group_members` RPC + `getSpotGroupMembers` 클라이언트 함수.
   - [x] `DetailModal`에 "이 장소의 다른 예약 옵션 보기" 버튼(스팟픽 지도 화면 한정) + 기존 `MarkerGroupModal` 재사용해 펼쳐보기.
   - **[특이 사항]** 코드/메커니즘은 특정 카테고리에 종속되지 않는 일반 구조로 구현했다. 다만 이 기록 완료 시점 기준 `spot_dedup_groups`는 여전히 0건이라(실제 그루핑은 관리자가 위 1번 도구로 별도 수행해야 함), 난지캠핑장 사례를 화면에서 직접 확인하려면 관리자가 그 도구로 먼저 그룹을 등록해야 한다 — 사용자가 제안한 "일단 캠핑장부터" 범위는 코드가 아니라 이 실제 등록 작업의 범위로 남아있다.
- [x] 3. 노출 중분류별 중복 스팟 검수 + 기존 그룹 자동 편입 + 한시성 예약 스팟 자동 삭제 — [[2026-09-09-category-scoped-dedup-and-auto-expiry]]
   - [x] "먼저 노출중분류 선택하고 거기 있는 데이터들끼리만 좌표 비교해서 중복 스팟 있는지 확인" — `find_spot_dedup_candidates` RPC에 `p_service_category_id`(선택) 추가, `SpotDedupPanel`에 "스캔 범위" select(노출 중분류 | 미매핑 원본 전체=기존 방식) 추가.
   - [x] "일단 중복되는 것에 대하여 대표스팟을 만들고 추후에 들어온 데이터들도 동일 좌표면 대표스팟내로 묶이는걸로 하자" — 신규 `auto_assign_open_spaces_to_existing_groups` RPC, daily batch(DEDUPE_OPEN_SPACES 다음 단계)에서 매일 자동 실행. 이미 사람이 확정한 그룹의 좌표 30m 이내 신규 미그룹 행만 편입(새 자동 판정 휴리스틱 아님).
   - [x] "seoul_public_reservation으로 들어온것들은.. 예약일자 기준 end date가 지난건 open_spaces에서 삭제해버리자" — `deleteExpiredReservationSpaces`(raw_data.SVCOPNENDDT 기준, deactivateExpiredEvents와 동일한 유예 0일 정책), daily batch에 추가. 실측(dry-run): 1,465건 중 279건이 이미 만료 상태로 확인(다음 배치에서 실제 삭제 예정).
   - **[특이 사항]** 난지캠핑장 42건은 아직 미매핑 상태라 "노출 중분류 선택 스캔" 모드로는 못 찾는다 — 관리자가 먼저 캠핑장 중분류로 매핑해야 하며, 이 매핑 작업은 이번 범위 밖이라 진행하지 않았다(기존 "미매핑 원본 전체" 모드로는 지금도 검수 가능).
- [x] 4. 중복 스팟 병합 모달 필드 정리 + 중분류 선택 초기화 버그 수정 — [[2026-09-09-dedup-merge-modal-field-cleanup]]
   - [x] "표준 시설명말고 중분류나 블로그 URL(선택)? 연령대? 특징? 이런건 왜있어?.. 블로그 연령대 특징 같은건 나중에 블로그 큐레이션에서 넣는걸로 하고 여기서는 빼는게 낫지 않을까?" — 실측 확인 결과 셋 다 앱 어디서도 읽히지 않는 사문화 필드(블로그 큐레이션의 blog_url_1/2/3+curation_badges가 이미 동일 역할 수행)라 셋 다 모달/apply route에서 제거(새 블로그 검색 UI는 만들지 않음, DB 컬럼 자체는 미변경).
   - [x] "중분류는 이미 중분류 선택한거에 대하여 하는거라 선택안함 상태로 하면 중분류 한게 다시 선택안함으로 변하는거 아니야?" — 실제 버그였음을 확인, `GroupDetailModal`에 `initialServiceCategoryId` prop 추가해 스캔 범위/스팟의 기존 service_category_id로 미리 채움(3개 진입점 모두: SpotDedupPanel/SpotDedupQuickModal/MobileCurationWorkbench).

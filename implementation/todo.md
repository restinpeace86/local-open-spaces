## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---
# To-Do List

# [Task] AI 챗봇 지역 선택, 이벤트픽 바텀시트 필터링, 그리고 캠핑장 데이터 이벤트픽 연동 수정 (개선사항 2, 3 & 4)

현재 서비스 내에서 발견된 AI 챗봇의 지역 선택 UX/로직 문제, 이벤트픽 화면의 카테고리 필터링/바텀시트 연동 오류, 그리고 공간시설 데이터(`open_spaces`)의 이벤트픽 카테고리 누락 문제를 아래 상세 요구사항에 맞춰 모두 수정해 주세요.

---

## 🛠️ [개선사항 2] AI 챗봇 지역 선택 UX 및 로직 개선

1. **'지역 목록에서 선택' 버튼 상시 노출 (승격)**
   - 기존에는 GPS가 실패했을 때만 간헐적으로 노출되던 "🗺️ 지역 목록에서 선택" 버튼을, 지역 설정 단계에서 **항상 상시 노출(Promote)**되도록 UI를 수정해 주세요.

2. **'다른 지역' 목록 로딩 및 동작 오류 수정**
   - 현재 챗봇 플로우에서 "다른 지역"을 선택하여 목록을 불러오고 챗봇 세션에 적용하는 과정에서 제대로 작동하지 않는 버그나 누락된 핸들러 로직을 찾아 수정해 주세요.

3. **시/군/구 광역 단위 선택지 확장**
   - 너무 세부적이거나 모호했던 선택지 대신, 사용자가 직관적으로 고를 수 있는 **[시/도] + [시/군/구] 광역 단위** (예: `서울특별시 광진구`, `경기도 성남시`, `경기도 용인시`, `서울특별시 마포구` 등)의 명확하고 넓은 선택지 리스트를 구성하고 모달/셀렉트에 연동해 주세요.
   - 해당 선택으로 지정된 지역은 **현재 챗봇 세션(session)에만 적용**되어야 합니다.

---

## 🛠️ [개선사항 3] 이벤트픽 대분류-중분류 바텀시트 및 카드 필터링 연동 수정

1. **대분류 클릭 시 바텀시트 동작**
   - 이벤트픽 화면 하단에 위치한 대분류 버튼을 누를 때, 하위 중분류 목록을 담은 바텀시트(Bottom Sheet)가 정상적으로 슬라이드되어 올라오는지 확인하고 수정해 주세요.

2. **중분류 선택 시 이벤트 카드 필터링**
   - 바텀시트 내부에서 특정 **중분류를 선택했을 때**, 바텀시트가 닫히면서 **해당 중분류(또는 대분류+중분류)에 해당하는 이벤트 카드들이 이벤트픽 화면에 실시간으로 필터링되어 렌더링**되도록 연동해 주세요.
   - 현재 선택된 카테고리 상태(State)가 이벤트 카드 목록 조회 쿼리나 상태 필터에 제대로 반영되지 않고 있거나 엉뚱한 곳에서 렌더링되는 문제를 해결해 주세요.

---

## 🛠️ [개선사항 4] open_spaces 캠핑장 데이터의 이벤트픽 연동

# [Task] open_spaces 기반 캠핑장 및 농장체험 데이터를 이벤트픽 카테고리로 공유 및 연동 (개선사항 4)

기존 `open_spaces` 테이블에 존재하는 공간/시설 데이터 중 아래 3가지 중분류 항목들을 이벤트픽 화면에서도 공통으로 조회할 수 있도록 쿼리, 필터, 그리고 UI 렌더링 구조를 연동해 주세요. (별도의 중복 테이블이나 데이터를 생성하지 않고 원천 데이터를 공유하는 방식입니다.)

## 🎯 상세 매핑 및 UI/UX 요구사항

1. **카테고리 매핑 구조**
   - **캠핑장 연동:** `open_spaces` 중분류 '캠핑장' ➔ 이벤트픽 대분류 **"자연 / 캠핑"** / 중분류 **"캠핑장"**
   - **체험휴양마을 연동:** `open_spaces` 중분류 '체험휴양마을' ➔ 이벤트픽 대분류 **"체험 / 농장"** / 중분류 **"체험휴양마을"**
   - **교육농장 연동:** `open_spaces` 중분류 '교육농장' ➔ 이벤트픽 대분류 **"체험 / 농장"** / 중분류 **"교육농장"**

2. **카드 UI 및 렌더링 규칙**
   - 원천 데이터가 `open_spaces`에서 오더라도, **화면에 보여지는 카드 컴포넌트는 철저하게 이벤트픽 카드 UI 규격**에 맞춰 렌더링해 주세요.
   - 이벤트픽 카드에 필수적인 날짜 정보가 `open_spaces` 데이터에는 존재하지 않으므로, 상시 운영 성격의 공간들은 날짜 대신 **`[상시]` (또는 `[상시 운영]`) 뱃지**를 자연스럽게 표시해 주세요.

3. **상세 페이지 및 라우팅 연동**
   - 사용자가 이벤트픽에서 해당 카드를 클릭했을 때, 이질감 없이 **이벤트픽의 상세 화면 및 예약/안내 흐름**으로 매끄럽게 진입할 수 있도록 라우팅을 연결해 주세요.

---
위 매핑 및 렌더링 규칙에 따라 사용자가 이벤트픽 화면에서 해당 카테고리들을 선택했을 때, 데이터가 이벤트픽 스타일의 카드들로 정확히 필터링되어 노출되도록 구현해 주세요.
# [Task] 어드민(Admin)을 통한 스팟픽(`open_spaces`) ➔ 이벤트픽 테이블 간 데이터 이동(마이그레이션) 기능 구현 (개선사항 5)

원본 공공데이터의 분류가 잘못되어 스팟픽 테이블(`open_spaces`)에 들어와 있는 데이터 중, 실제로는 이벤트나 체험 프로그램 성격인 항목들을 **관리자가 직접 이벤트픽 테이블 쪽으로 완전히 이동(마이그레이션)**시킬 수 있는 어드민 기능을 구현해 주세요.

## 🎯 상세 요구사항

1. **어드민 관리 페이지 UI 추가**
   - 관리자 페이지의 스팟픽 관리 화면에서, 특정 스팟을 선택하고 **[이벤트픽으로 데이터 이동]**을 실행할 수 있는 액션 버튼 및 모달(또는 폼)을 만들어 주세요.
   - 이동 시 이벤트픽의 대분류 및 중분류를 직접 선택할 수 있어야 합니다.

2. **테이블 간 데이터 이동(트랜잭션 마이그레이션) 로직**
   - 관리자가 이동을 확정하면, 백엔드(또는 API/Server Action)에서 트랜잭션 처리를 통해 다음 작업이 수행되도록 구현해 주세요.
     1. `open_spaces` 테이블에 있던 해당 데이터의 필드 정보(이름, 위치, 설명, 이미지 등)를 읽어와 **이벤트픽 전용 테이블**의 스키마 규격에 맞춰 새로운 레코드로 `INSERT` 합니다.
     2. 이때 관리자가 선택한 이벤트픽 대분류/중분류 값을 올바르게 매핑합니다.
     3. 이동이 성공적으로 완료되면, 기존 스팟픽 테이블(`open_spaces`)에 있던 원본 데이터는 삭제(또는 소프트 딜리트/비활성화)하여 데이터가 양쪽에 중복으로 남지 않고 **완전히 이관**되도록 처리합니다.

3. **결과 확인**
   - 이관된 데이터는 즉시 이벤트픽 화면과 해당 중분류 바텀시트 필터에서 이벤트픽 카드 형태로 정상 노출되어야 합니다.

개선사항 4: open_spaces의 캠핑장, 체험휴양마을, 교육농장은 테이블 이동 없이 중분류 쿼리 공유를 통해 이벤트픽 화면에서도 함께 노출되도록 연동.
개선사항 5: 스팟픽에 잘못 들어온 진짜 이벤트성 데이터들을 어드민에서 골라 이벤트픽 테이블로 레코드 자체를 이동(Migration)시키는 기능 구현 (이동 시 기존 이벤트픽 대/중분류 선택 가능).

위 모든 개선사항들의 상세 요구사항들을 누락 없이 충족하도록 관련 컴포넌트, 상태 관리, 쿼리 및 필터 로직을 전면 점검하고 수정해 주세요.

---

# [Task] 스팟픽 지도 4대 대분류 필터 개편, 바텀시트 연동 및 거리순 정렬/성능 최적화 (개선사항 6)

스팟픽 지도 화면의 카테고리 필터 구조를 유저 친화적으로 대폭 개편하고, 중분류 선택 시 거리순 정렬과 성능 최적화가 함께 이루어지도록 아래 요구사항을 구현해 주세요.

## 🎯 상세 요구사항

1. **스팟픽 대분류 필터 노출 제한 및 순서 재배치**
   - 기존 대분류 중 **`체육시설`**, **`공공청사 대관`**, **`기타`** 항목은 지도 필터에서 아예 노출되지 않도록 제외합니다.
   - 오직 아래 **4가지 핵심 대분류**만 노출되도록 UI를 개편합니다.
   - **노출 순서 (왼쪽부터):** 
     1. `키즈/놀이시설`
     2. `농장/체험`
     3. `자연/공원`
     4. `문화시설`

2. **바텀시트(Bottom Sheet) 연동**
   - 상단 4대 대분류 버튼을 클릭했을 때, 하위 중분류 목록들을 담은 바텀시트가 슬라이드업 형태로 부드럽게 올라오도록 구현해 주세요.
   - 바텀시트 내에서 특정 중분류를 선택하면, 해당 중분류에 속하는 스팟들만 지도와 리스트에 필터링되어 노출되도록 연결합니다.

3. **거리순 정렬 및 성능 최적화**
   - 중분류 선택 시 필터링된 스팟들은 현재 설정된 유저 위치를 기준으로 **가까운 순(오름차순)**으로 상단에 노출되도록 정렬 로직을 적용해 주세요.
   - 단, 전체 데이터 대상의 무리한 거리 계산으로 인한 타임아웃을 방지하기 위해, **시/군/구 또는 광역 권역(예: 서울시 + 경기도 등) 기준**으로 1차 범위 필터링(Bounding Box)을 거친 후 PostGIS KNN 연산을 태우는 방식으로 성능 최적화를 보장해 주세요.

## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---
# To-Do List

[개선사항1] 이벤트픽 카드 및 상세 화면 개선 계획
'상시' 태그 제거

open_spaces 연동 카드(캠핑장, 체험휴양마을 등)에 붙어 있던 '상시' 태그는 실제 운영 상황 및 예약 필요 여부와 맞지 않아 혼선을 주므로 카드 UI에서 완전히 제거합니다.

'현재 위치에서 X km' 거리 정보 프리뷰 패널 상시 노출 + 성능 최적화

노출 위치: 유저가 상세 페이지를 일일이 열어보지 않고도 목록 카드 단계에서부터 곧바로 거리를 파악할 수 있도록 프리뷰 패널에 상시 표시합니다.

성능 최적화 (Pre-computation): 화면 진입 시점(또는 위치 확정 시점)에 데이터 전체에 대한 거리를 단 한 번 일괄 산출하여 각 카드 객체에 미리 매핑해 둡니다. 렌더링 단계에서 매번 연산하는 실시간 로직을 배제하여, 카드가 많아져도 스크롤 및 진입 시 버벅이지 않도록 가볍게 유지합니다.

부정확한 예약 안내("예약불필요" / "현장 방문") 정돈

정보가 불충분한 상태에서 유저에게 잘못된 정보를 줄 수 있는 "예약불필요" 및 "현장 방문" 뱃지/문구는 실정에 맞지 않으므로 과감하게 수정하거나 제거하여 신뢰도를 높입니다.

버튼 이름 변경

상세 모달 내 기존 '지도에서 보기' 버튼명을 '지도에서 길찾기'로 직관적으로 변경합니다.

---

[개선사항2] Hero 메인 카드 상단 뱃지 복구 및 영역별 타이틀 변경

메인 카드 이미지 상단에 '오늘 한정' / '오늘 마감' 등의 상태 뱃지를 다시 선명하게 노출시켜, 왜 이 카드가 메인에 자리를 잡았는지 유저가 직관적으로 인지할 수 있도록 원상 복구합니다.

'오늘 가능' 영역 중복 제거 (Deduplication)

Hero 메인 카드에 노출되는 '오늘 한정' 및 '오늘 마감' 대상 데이터는 하단의 '오늘 가능' 목록 영역에서 자동으로 필터링하여 제외시킵니다.

이를 통해 같은 데이터가 위아래로 중복 노출되는 혼선을 깔끔하게 없애겠습니다.

'현재 이용 가능' 영역 👉 "지금 이 순간 함께하기 좋은 알찬 픽"

'예약 가능' 영역 👉 "놓치면 후회하는 인기 만점 예약 픽"

---

[개선사항3] 카드 컴포넌트 및 규격 동기화

메인 이벤트픽 목록에서 사용하는 표준 카드 UI(컴팩트한 비율, 거리 정보 패널, 뱃지 정리된 형태)를 전체보기 페이지/모달의 그리드에도 동일하게 적용합니다.

기존의 세로로 길쭉하던 구형 카드 스타일을 제거하고, 메인 화면과 완벽히 동일한 크기와 레이아웃으로 렌더링되도록 맞춤 정렬합니다.

이벤트픽 화면 각 영역에 대하여 전체보기로 보는 바텀시트에 대하여서도 마찬가지로 페이지네이션(또는 적절한 단위의 끊어 읽기/무한 스크롤 등)을 도입하여 응답 속도를 가볍고 빠르게 최적화합니다.

---

[개선사항4]  이벤트픽 카드 목록 뱃지 최종 원칙
상단 좌측 뱃지: 중분류 뱃지 (유지)

상단 우측 뱃지: 예약 상태 뱃지 (예약마감, 접수중 등) (유지)

하단 뱃지: 실내/야외, 무료/유료 (유지)

원칙: 목록 카드에서 보여주는 뱃지는 딱 여기까지만 노출합니다.

추가 편의 정보: 추후 추가될 주차 가능, 유모차 대여 여부 등의 부가 뱃지들은 목록을 복잡하게 만들지 않도록 상세 보기 페이지(모달) 내부에서만 확인할 수 있도록 분리합니다.

🔍 '무료/유료' 분류 데이터 점검 진행
말씀하신 대로 원천 데이터나 매핑 로직에서 무료인데 유료로 표기되거나, 반대로 유료인데 무료로 잘못 분류된 케이스가 없는지 무료/유료 분류 체계를 다시 한번 철저하게 검증하고 보완하겠습니다.

---

[개선사항5] 프로필 입력(/auth/complete-profile) 출생년도 드롭박스 개편안

입력 방식 변경:
자유 타이핑 방식 ➔ 직관적인 드롭박스(Select 콤보박스) 선택 방식으로 전면 전환.
선택 연도 범위 설정 (타겟 맞춤):
상한선: 최근 태어난 영유아/아이들을 포함하여 2026년생(또는 최근 연도)부터 시작.
하한선: 서비스 타겟인 초등학교 6학년 기준인 2014년생까지 선택 가능하도록 구성.
(예시 드롭박스 노출 형태: 2026년생, 2025년생 ... 2014년생 (초등 6학년)) 형식으로 유저가 나이와 연도를 한눈에 알아보기 쉽게 정리
기대 효과:
잘못된 연도가 입력되는 휴먼 에러를 원천 차단하고, 모바일 환경에서 탭 한 번으로 간편하게 입력을 마칠 수 있어 가입 및 프로필 완성 전환율을 높임.

봇 추천 및 페르소나 프롬프트 규칙
날씨/나들이 추천 단어 필터링:
"공원"이라는 단어와 픽스된 제안은 전면 배제.
이벤트픽의 6대 대분류 성격에 맞춰 "야외 이벤트" 또는 "야외 나들이" 위주로 자연스럽게 제안.
다자녀/동갑 아이 나이 멘트 최적화:
아이들의 출생년도가 같거나 쌍둥이인 경우, "3살, 3살 아이들"처럼 기계적으로 반복하여 출력하지 않음.
"3살 두 아이" 또는 "동갑인 두 아이(3살)" 형태로 자연스럽게 묶어서 멘트가 생성되도록 응답 로직 규칙 적용.

챗봇 지역 선택(시군구) 검색 필터 기능 추가 (New)
검색 입력창 제공: 시군구 선택 리스트가 뜨면 상단에 실시간 키워드로 해당 리스트 내에서 재검색할 수 있도록 바 추가.
실시간 필터링: 긴 리스트에서 스크롤할 필요 없이, 지역명을 입력하면 즉시 일치하는 시군구만 필터링되어 노출되도록 구현.
글자를 한 자 한 자 입력할 때마다 리스트가 실시간으로 반응하여 일치하는 지역명만 필터링되어 노출되도록 처리.

챗봇 이용 제한 안내 시점 전면 개선 (New)
진입 시점(Entry Point) 체크 원칙:
챗봇을 다 완료한 시점이 아니라, 유저가 챗봇을 처음 켤 때(입장하는 순간) 이용 횟수 소진 여부 및 후기 작성 상태를 먼저 검증.
헛고생 방지: 이용 권한이 없는 경우(후기 미작성 등) 질문/선택 단계를 시작하기도 전에 안내 팝업이나 첫 화면에서 바로 유도하여, 유저가 불필요한 입력을 반복하는 피로감을 원천 차단.
비로그인 유저의 경우 1회 횟수 소진 시 -> 로그인 및 회원가입 유도
로그인 유저의 경우(후기 미작성으로 인하여 이용 권한 없을 시) -> 후기 작성할 수 있는 곳으로 유도

스팟픽 올바른 중분류 바텀시트 및 데이터 연동 구조

시트 유지 및 내부 탭핑 구조 (Persistent Sheet with Internal Tabs)
대분류를 눌러서 뜬 바텀시트(혹은 필터 패널)는 닫히지 않고 그대로 유지됩니다.
시트 상단(또는 고정 영역)에는 해당 대분류에 속한 여러 개의 중분류 칩/버튼들이 나란히 위치합니다.

동적 데이터 연동 (Dynamic Content Update)
유저가 중분류 A를 누르면 ➔ 바텀시트 내부 하단 영역에 중분류 A에 해당하는 데이터 목록이 뜸.
유저가 바로 옆의 중분류 B를 누르면 ➔ 시트는 그대로 열려 있는 상태에서, 하단 데이터 목록만 중분류 B에 맞는 내용으로 즉시 전환(업데이트)됨.
레이아웃 및 스크롤 안정화 (No Clipping)
상단의 중분류 선택 영역은 스크롤해도 제자리에 고정(Sticky)되거나 상단에 잘 머물러 있고, 그 아래의 데이터 목록 영역만 스크롤되도록 처리하여 리스트가 길어져도 시트 영역을 뚫고 짤리거나 깨지지 않도록 견고하게 잡습니다.

---

최종 완성된 유저 저니 (User Journey) 플로우
앱 최초 진입 & 위치 설정

앱을 켜자마자 자연스럽게 위치 설정부터 먼저 완료하고 홈 화면 진입.

비로그인 탐색 및 챗봇 1회 체험

로그인 없이 편하게 둘러보다가 챗봇을 1회 가볍게 체험.

챗봇 2회 차 시도 ➔ 회원가입 전환 벽

챗봇을 한 번 더 쓰려고 하는 순간(또는 제한 도달 시), 자연스럽게 로그인(카카오/구글) 유도.

초간소화 온보딩 (이탈 최소화)

소셜 로그인 직후 ➔ 닉네임 + 아이 출생년도(드롭박스 선택, 2014~2026년, 쌍둥이/다자녀 동시 대응) 입력 끝!

가입 직후 '새싹맘 등업' 팝업 (핵심 전환점)

가입 축하 메시지와 함께 곧바로 브레이크: "앗, 회원가입을 환영해요! 첫 후기 1개를 남겨 '새싹맘'이 되면 지금 바로 챗봇을 무제한으로 이용하실 수 있어요!" 팝업 노출.

[첫 후기 쓰러 가기] 버튼 클릭 시 곧바로 맘스픽 글쓰기 화면으로 직행.

고품질 리뷰 작성 완료 ➔ 챗봇 무제한 해제

결과: 등록 즉시 '축하합니다. 새싹맘으로 등급 업하셨습니다.' 팝업 노출. 
--> 이제 챗봇 무제한 이용 권한이 락(Lock) 해제되며 본격적인 서비스 루프 안착!

[글쓰기 화면 구조]
[설문형 스마트 리뷰 폼]
1단계: 어느 스팟인가요? (장소 선택)

후기 작성 페이지에 들어오는 순간, 위치 기반으로 내 주변에서 지금 가장 인기 있는 장소들이 카드나 리스트 형태로 바로 뜹니다.
유저는 글자를 칠 필요도 없이, 다녀온 곳이 보이면 탭 한 번(Touch)으로 장소 선택을 끝냅니다. (입장 마찰 제로)
타이핑 없이 '내 주변 인기 스팟' 리스트에서 탭 한 번으로 선택 (필요시 검색).

2단계: 아이와 함께 어땠나요? (구글 폼 스타일 설문/체크리스트)

유저가 긴 글을 쓸 필요 없이, 직관적인 선택지들을 터치(클릭)하며 빠르게 응답하도록 구성합니다.

주요 문항 예시:

이 장소는 몇 세 아이와 가기 가장 좋았나요? (체크박스/다중선택: 영유아 / 미취학 / 초등 저 / 초등 고)

방문 환경은 어땠나요? (라디오버튼: ☀️ 탁 트인 야외 / 🏠 쾌적한 실내 / ⛺ 복합)

부모 입장에서 가장 만족스러웠던 점은? (복수 선택: 주차 편리 🚗 / 유모차 이동 수월 🦽 / 아이가 지루해하지 않음 🧸 / 가성비 좋음 💳 등)

3단계: 딱 한 줄 생생 꿀팁 + 사진 (선택 보완)

설문만으로는 담기 아쉬운 디테일을 위해, 하단에 가이드가 적힌 짧은 입력창 + 사진 첨부(선택)를 가볍게 배치합니다.

가이드 문구: "다른 부모님들에게 전할 나만의 꿀팁이나 주의할 점이 있다면 남겨주세요! (선택)"

설문으로 뼈대 데이터(정형 데이터)를 다 먹었기 때문에, 텍스트는 짧아도 플랫폼 자산으로 활용하는 데 전혀 지장이 없습니다.


앞서 확정한 '내 주변 인기 스팟' 추천 로직(위치 기반 + 플랫폼 내 인기 순)이 작동하는 장소 풀(Pool)은 우리 서비스의 기존 이벤트픽 데이터와 운영진이 직접 검증/큐레이션한 스팟 리스트로 한정합니다.
대상: 위치 설정 기준 30km 이내로 한정
공공/대형 어린이 공원 (단순 동네 놀이터 제외)
체험형 박물관, 미술관, 과학관
키즈카페, 실내 놀이터
캠핑장, 자연휴양림
각종 지자체 주관 체험 행사/프로그램
아이 동반 특화 문화 공간 (도서관 어린이실 등)

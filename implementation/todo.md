## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---
# To-Do List

[개선사항 1] 관리자 워크벤치 내에 [키즈카페 / 실내놀이터] 카테고리를 위한 스팟 큐레이션 팝업 UI 및 로직을 구현해 주세요. 기존 '키즈친화 식당' 팝업의 컴포넌트 구조를 베이스로 재사용하되, 다음 요구사항을 반영해 주세요.
'키즈카페 / 실내놀이터' 팝업 UI 구축
방금 정리한 세미오토 뱃지 검수(Opt-out), 가격 스마트 파싱, 옵셔널 메뉴가 들어가는 검수 팝업 적용
목적: 실내 키즈카페와 공공 실내놀이터 데이터를 빠르고 정확하게 정제하기 위함.

- [x] 1. 뱃지 세미오토 검수 (Opt-out) 구조
   - [x] 블로그 본문 및 키워드 데이터를 기반으로 시스템이 주요 뱃지(예: 주차 완비, 놀이방, 아기의자 등)를 1차로 자동 체크(Pre-check)해 주어야 합니다. — 2026-09-07 개선사항3-4로 이미 구현 완료(`matchBadgeKeysFromText`).
   - [x] 관리자는 이를 눈으로 검수하고, 문맥상 맞지 않는 항목은 클릭하여 간편하게 체크를 해제(Opt-out)할 수 있어야 합니다. — 위와 동일(`toggleBadge`).
   - [x] 트램폴린에 대하여 키워드 트램펄린도 가져가도록 할것. — [[2026-09-08-trampoline-synonym]] `curation-badges.ts`의 `kc_trampoline` 키워드에 '트램펄린' 추가.

- [ ] 2. 팝업 기본 입력 필드 (식당 팝업 구조 재사용)
   - [ ] 대표 이미지 등록
   - [ ] 영업시간 및 휴무일 정보

- [ ] 3. 가격 및 입장료 스마트 파싱 기능
   - [ ] 네이버 플레이스 등의 가격 텍스트를 그대로 복사·붙여넣기(Paste)할 수 있는 [가격 스마트 입력창]을 제공해 주세요.
   - [ ] 내부 파싱 로직(정규식 등)을 통해 어린이 요금, 보호자 요금 등의 필드에 숫자가 자동으로 쪼개져 매핑되도록 하고, 관리자가 수정·저장할 수 있어야 합니다.

- [ ] 4. 식음료 및 메뉴 정보 (옵셔널 처리)
   - [ ] 키즈카페 내 판매하는 음료/간식 정보를 적는 메뉴 입력란을 두되, 필수가 아닌 [옵셔널(선택 입력)]로 처리하여 빈칸이어도 에러 없이 저장되도록 구현해 주세요.

[개선사항 2] 관리자 워크벤치의 뱃지 자동 체크(Pre-check) 및 데이터 연동 로직을 다음과 같이 고도화해 주세요.

- [x] 1. 뱃지 상태별 시각적 색상 구분 (UI/UX) — [[2026-09-08-badge-color-state]]
   - [x] AI 시스템이 키워드 기반으로 1차 자동 체크를 수행했으나 아직 DB에 저장되지 않은 상태: **초록색 (Green)** 뱃지로 표시
   - [x] 기존에 이미 DB에 저장되어 불러와진 상태: **파란색 (Blue)** 뱃지로 표시
   - [x] (기존의 단순 검은색 표시를 위 상태에 맞게 변경) — `bg-gray-900`(검정) 전부 제거, 미체크는 기존 흰색/회색 그대로.

- [ ] 3. 멀티 블로그 키워드 종합 분석 및 워닝(Warning) 필터링
   - [ ] 현재 블로그 1의 키워드만 참고하는 구조를 수정하여, **블로그 1, 2, 3 등 연결된 전체 블로그의 키워드 및 본문**을 종합하여 뱃지를 자동 체크하도록 확장해 주세요.
   - [ ] 단, **워닝(Warning)이 걸린 블로그는 키워드 분석 대상에서 제외(Drop)**해야 합니다.
   - [ ] *워닝 판정 기준:* 탭 제목에 워닝 아이콘이 부착되어 있거나, 제목/본문 내에서 필수 지역 키워드를 찾지 못한 블로그 등은 신뢰도가 낮으므로 뱃지 자동 체크 계산에서 제외하고, 오직 정상 상태인 블로그들의 데이터만 취합하여 1차 뱃지를 체크해 주세요.

[개선사항 3] 유저 서비스 (스팟픽) UX/UI 구현

- [~] 1. **지도 및 정렬 (Wide-view & Distance Sort)**
   - [ ] 중분류는 우리가 정한 노출 대분류와 노출 중분류로 합니다. 현재 대분류 중분류는 다 걷어내고 노출 중분류 기준으로 보여줍니다.
   - [스킵 (보류)] 특정 반경(예: 3km 등)으로 핀을 제한하는 컷오프를 없애고, 선택된 중분류에 해당하는 서울/경기 전역의 핀을 지도에 넓게 노출합니다. (도 단위, 경기도, 강원도, 전라도, 경상도 등으로 설정된 위치에 따른 도 전역의 데이터 노출)
     - **① 상세 스킵 사유(2026-09-08 확인):** 승인된 `spec/map/spatial-search.md` §2.1(탐색 범위)이 "제공 옵션: 1km/5km/10km(기본 5km)", "10km를 초과하는 탐색은 무분별한 서버 부하 방지를 위해 제한하며, 단일 반경 검색 모드를 기본으로 한다"를 명시하고, §3.1(렌더링 개수 제한)도 "반경 내 1,000개 초과 시 최상위 1,000개만 렌더링 + 초과 안내 토스트"로 반경 기반 개수 제한을 전제로 설계돼 있다. `project/decision-log.md` Decision 010과 이번에 갱신된 spec §2.2는 "지도 레이어를 상시공간/이벤트로 분리"한 것이지 "반경 컷오프 자체를 없앤다"는 내용이 아니다 — 이 지시(반경 제한 완전 폐지 + 도 전역 노출)는 §2.1/§3.1과 정면으로 충돌한다(제3장 제2조 Spec 우선, 제7장 제1조 Spec 없는 기능 추가 금지). 도 전역 노출 시 대분류 하나에 수만 건(예: 어린이놀이터 57,692건 실측)이 잡힐 수 있어 §3.1의 "반경 기준 1,000개 초과 안내" 로직 자체도 재설계가 필요하다 — 임의로 판단해 구현하지 않는다.
     - **② 선행되어야 할 작업:** `project/decision-log.md`에 "스팟픽 지도 반경 컷오프 폐지 + 노출 중분류 기준 도 전역 노출"을 명시한 신규 Decision을 먼저 기록하고, `spec/map/spatial-search.md` §2.1/§3.1을 그 Decision에 맞춰 개정(반경 컷오프 없는 전역 렌더링 시의 마커 개수 제한/클러스터링 전략을 새로 정의)한 뒤 재개한다.
   - [ ] 하단 바텀시트 리스트는 유저의 **'현재 GPS 위치'를 기준으로 가까운 거리순**으로 정렬합니다. (단 하단 바텀시트 리스트는 현재 설정한 위치 기준 반경 10km 로 제한합니다.) — 10km는 §2.1의 기존 허용 범위 내라 충돌 없음. 단, 지도 자체는 위 스킵 항목 때문에 반경 제한을 유지한 채로 구현한다(바텀시트 정렬/컷오프만 별개로 적용 가능).
- [ ] 3. **실시간 위치 싱크 (GPS Sync 팝업)**
   - [ ] 유저가 사전에 설정한 동네와 현재 실제 GPS 위치 간에 유의미한 차이가 감지될 경우, *"현재 위치({동네 이름})로 위치를 변경할까요?"* 형태의 팝업을 띄우고, 수락 시 기본 위치 설정을 현재 GPS 기준으로 업데이트합니다.
- [ ] 4. **마커 및 바텀시트 인터랙션 (Preview ➡️ Detail Flow)**
   - [ ] **1단계 (프리뷰):** 지도 핀 또는 바텀시트 리스트를 누르면, 지도가 해당 위치로 이동하며 하단에 **[요약 프리뷰 카드]** (대표 이미지, 이름, 핵심 뱃지)가 팝업됩니다.
   - [ ] **2단계 (상세 뷰):** 프리뷰 카드를 터치하면 전체 화면의 **[상세 페이지]**로 확장 진입합니다. 여기에 영업시간 / 가격 / 메뉴 등의 정보들이 들어있음.
- [ ] 5. **가격 및 메뉴 '준비 중' 플레이스홀더 처리**
   - [ ] MVP 단계에서 가격이나 메뉴 데이터가 아직 채워지지 않은 스팟의 경우, 빈 화면 대신 아래와 같은 플레이스홀더 문구를 노출하여 신뢰도를 유지합니다.
     - 가격 영역: *"가격 정보 업데이트 준비 중입니다 ⏳"*
     - 메뉴 영역: *"상세 메뉴 정보는 순차적으로 추가될 예정이에요"*
     - 영업시간 영역: *"영업시간 정보는 순차적으로 추가될 예정이에요"*

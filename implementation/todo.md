## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---
[스킵 (보류)] [개선사항 1] 당신은 Next.js App Router, TypeScript, Supabase, 그리고 Playwright를 활용한 웹 크롤링 및 백엔드 파이프라인 구축에 능숙한 시니어 풀스택 엔지니어입니다.

> **① 상세 스킵 사유 (2026-09-16)**: 착수 전 대상 사이트(요청 원문 예시
> "마이리얼트립 등")를 직접 확인한 결과, myrealtrip.com의 `robots.txt`가 개별
> 상품 상세 페이지 크롤링을 **모든 User-Agent에 대해 명시적으로 금지**하고
> 있음을 확인했다:
> ```
> User-agent: *
> Disallow: /
>
> User-agent: Googlebot
> ...(Bingbot/DAUM/facebookexternalhit 등 명시된 봇들도 동일)
> Allow: /offers
> Disallow: /offers/*/     ← 목록 페이지는 허용, 개별 상세 페이지는 금지
> Allow: /accommodations/stays
> Disallow: /accommodations/stays/*/   ← 동일 패턴
> ```
> 즉 Googlebot처럼 명시적으로 이름이 등재된 검색엔진 봇들조차 개별 상품
> 상세 페이지(`/offers/{id}/` 등)는 크롤링이 금지돼 있다 — 목록/검색 페이지만
> 허용된다. 이 기능은 정확히 그 금지된 상세 페이지를 관리자가 붙여넣은 URL
> 그대로 자동 크롤링하는 것이 목적이라, 브라우저 User-Agent를 흉내 내 이
> 정책을 우회하는 형태로만 구현이 가능하다 — 대상 사이트가 스스로 공표한
> 접근 정책을 자동화 코드가 회피하는 것은 기술적 문제가 아니라 ToS/정책 위반
> 리스크이며, "안 되는 걸 되는 척 구현하지 않는다"는 이 프로젝트의 기존
> 원칙(2026-08-19, SEOUL_YEYAK 동적 크롤링 보류 사례와 동일한 성격)과 같은
> 이유로 임의로 진행할 수 없다(제3장 제5조 추측 금지 — 회피가 승인됐다고
> 추측하지 않음).
> 부가적으로, myrealtrip은 Next.js 기반이지만 실제 가격/옵션 정보는 클라이언트
> 하이드레이션 이후 렌더링되는 것으로 보여(홈페이지 정적 HTML에는 상품 링크
> 자체가 없음), robots.txt 문제가 없더라도 Playwright 등 헤드리스 브라우저가
> 필요할 가능성이 높다 — 이는 이 프로젝트 프로덕션 런타임(Vercel 서버리스)에
> 아직 없는 무거운 신규 의존성이라는 점도 위 SEOUL_YEYAK 사례와 동일하다.
> 요청 원문이 "마이리얼트립 등"이라 특정 사이트 하나의 문제가 아니라, 임의의
> 제휴사 URL을 다 크롤링하는 범용 기능 자체가 사이트마다 다른 접근 정책과
> 충돌할 위험을 구조적으로 안고 있다.
>
> **② 재개 선행 작업**: 아래 중 하나가 필요하다.
> 1. 실제로 지원해야 하는 제휴사 목록을 확정하고, 각 사이트의 robots.txt/
>    이용약관을 개별 확인해 크롤링이 허용되는 사이트만 대상으로 범위를
>    좁히는 결정을 `project/decision-log.md`에 기록, 또는
> 2. 마이리얼트립 등 대상 사이트가 공식 제휴/어필리에이트 API·데이터 피드를
>    제공하는지 확인해(제공한다면 그 공식 채널을 쓰는 것이 스크래핑보다
>    안전하고 안정적이다) 그 경로로 재설계, 또는
> 3. 정책 위반 리스크를 인지한 상태에서도 사업적으로 진행하기로 결정했다면,
>    그 판단과 책임 소재를 `project/decision-log.md`에 명시적으로 기록한 뒤
>    재개(제4장 제5조 — 역할 침범 금지: 이런 리스크 수용 판단은 구현 AI가
>    아니라 서비스 운영 주체가 내려야 한다).
>
> **재개 (2026-09-16)**: 사용자가 위 ②번 경로로 직접 진행 — "개선사항1은
> 포기, 대신 MYREALTRIP_API_KEY 발급받음. 공식적인 API를 통하여 데이터
> 가져오는 방법 확인해보자". robots.txt 크롤링과 무관한 승인된 파트너 API라
> 정책 충돌 없음을 확인하고, categories/search 2개 엔드포인트로 관리자 신규
> 탭("🔍 마이리얼트립 상품 검색")을 구현 — 도시별 "키즈" 카테고리 필터로
> 실제 아동 대상 상품만 걸러 탐색하고, 마음에 드는 결과는 기존 큐레이션 등록
> 폼으로 바로 등록 가능. detail/options/calendars(상세·날짜별 예약가능여부)는
> 카테고리 범위 결정 이후 필요 시 추가 예정. 상세:
> implementation/2026-09-16-myrealtrip-partner-api-search.md
>
> **후속 (2026-09-16)**: "바로 반영해, 상품리스트 보고.. 상품 상세 들어가서
> 제휴상품으로 등록하는 흐름으로" — (1) 마이링크(`/v1/mylink`) 실측 확인 후
> 등록 흐름에 반영: 원본 productUrl 대신 마이링크(myrealt.rip 추적 링크)를
> booking_url로 저장하도록 변경(실측: 마이링크가 실제로 mylink_id/t_scope/
> utm_source가 붙은 페이지로 정상 리다이렉트됨을 확인). (2) 검색 카드 클릭 →
> 상세 모달(설명/포함·불포함 사항, `/v1/products/tna/detail`) → "🔗 제휴
> 상품으로 등록"(마이링크 생성) → 등록 폼 순서로 흐름 변경. 마이링크 생성
> 실패 시 원본 링크로라도 등록할 수 있는 대안(수익 정산 안 됨을 명시)도 남김.
> 상세: implementation/2026-09-16-myrealtrip-mylink-and-detail-flow.md

현재 관리자 화면의 '큐레이션/제휴 상품' 탭에서 '신규 상품 등록' 버튼을 클릭하여 나오는 팝업에서 추가적으로 **[URL 기반 자동 크롤링 및 오토필(Auto-fill) 팝업 모달]** 기능을 구현하려고 합니다.

아래 요구사항에 맞추어 프론트엔드 UI 컴포넌트와 백엔드 API(크롤러 연동) 코드를 작성해 주세요.

### [기능 요구 사항]
1. **관리자 입력 UI (Modal / 팝업 화면):**
   - 마이리얼트립 등 제휴 상품의 원본 URL을 입력할 수 있는 `Input` 필드와 **[데이터 가져오기]** 버튼 배치. (최상단)
   - 크롤링 진행 중인 상태를 보여줄 수 있는 로딩 상태(Loading Spinner 등) 처리.
2. **백엔드 크롤링 및 파싱 API (`/api/admin/crawl-preview` 등):**
   - 전달받은 URL을 Playwright(또는 적절한 파서)를 이용해 페이지에 접속 후 데이터 추출.
   - 추출할 데이터 필드: 
     * 상품명 (Title)
     * 대표 이미지 URL (Image)
     * 가격 정보 (Price / 옵션별 가격 등)
     * 요약 및 상세 설명 (Description)
     * 할인 정보 (카드 할인 등)
     * 제휴 상품 서비스 유효기간 / 운영 기간 
     *필수 사항:* 크롤링한 대표 이미지를 기존 프로젝트의 Supabase Storage 리사이징/재호스팅 파이프라인과 연동할 수 있는 구조 고려.
     *그 외 우리 상세 카드에 필요한 내용이 있으면 제안할 것
   - 현재 존재하는 입력 칸 중 제휴 링크(booking_url) / 연동 장소(Spot) — 선택은 관리자가 별도로 입력해야 하는 정보며 이번 크롤링하여 오토필하는 대상 데이터들과는 무관함.
3. **크롤링 결과 프리뷰 및 오토필 검수 UX:**
   - 크롤링이 완료되면, 모달 내의 폼 필드(상품명, 이미지 미리보기, 가격, 설명, 운영일, 예약일, 할인정보 등)에 데이터가 자동으로 쏙 채워지도록(Auto-fill) 구현.
   - 관리자가 긁어온 데이터를 눈으로 직접 확인하고, 필요시 수정(Edit)한 뒤 최종 등록 버튼을 누를 수 있도록 폼 구조 설계. (기존 블로그로 큐레이션과 유사한 구조로 크롤링된 데이터에 대하여 가독성 있게 정돈된 문장으로 제공되고 또 오토필을 위한 정보들에는 노란색 배경색 칠해져야함.)
4. **예외 처리:**
   - 잘못된 URL이 입력되었거나 크롤링 차단/타임아웃 발생 시 관리자가 알 수 있도록 명확한 에러 핸들링 추가.

### [출력 요구]
- 백엔드 API Route 핸들러 코드 (Playwright 활용 파싱 로직 포함)
- 관리자 등록 모달 컴포넌트 (React / TailwindCSS / 기존 프로젝트 컨벤션 기준)
- API 호출 및 오토필 상태 관리(State) 로직

--- 

[스킵 (보류)] [개선사항 2] [Feature Implementation Task] 이벤트 상세/카드 화면의 링크 버튼 우선순위(Fallback) 로직 구현

> **① 스킵 사유 (2026-09-15)**: 요청된 3단계(예약→상세/홈페이지→네이버검색)는
> `src/components/map/detail-modal.tsx`의 이벤트 상세 CTA 버튼 하나를 대체하는
> 구조인데, 이 버튼은 이미 **Decision 011**(공공 예약하기/할인 예매하기/길찾기
> 3분류, project/decision-log.md)로 공식 승인된 구조다. 확인 결과:
> - DB 스키마 자체는 이미 충족돼 있다 — `reservation_url`(예약 URL)과
>   `source_url`(공식 상세/홈페이지 URL, 2026-09-11 마이그레이션으로 이미
>   추가됨, `scripts/migrations/2026-09-11-events-price-text-source-url.sql`)이
>   요청한 두 컬럼과 정확히 대응한다. 신규 컬럼은 필요 없다.
> - 하지만 새 3단계를 그대로 적용하면 Decision 011의 "할인 예매하기"
>   (유료/민간 제휴 `affiliate_url`) 단계가 어디에도 들어갈 자리가 없어
>   사라지게 되고, 3순위 "길찾기"(인앱 지도, 2026-08-30 결정 "외부 지도 앱
>   연동 제거 및 인앱 위치 보기")도 네이버 검색 딥링크로 대체되어 없어진다 —
>   두 결정 모두 이 작업 지시에는 언급이 없어 "덮어써도 된다"는 승인으로
>   해석할 근거가 없다(제3장 제5조 추측 금지).
> - 참고로 스팟(open_spaces) 쪽에서는 2026-08-29 결정으로 "정보 없으면 네이버
>   검색 딥링크로 내보내는" 폴백을 의도적으로 **제거**하고 자체 신청 폼으로
>   대체한 전례가 있다 — 정반대 방향의 결정이 이미 한 번 있었다는 것도
>   이번 요청이 기존 UX 결정과 상충할 가능성을 뒷받침한다.
>
> **② 재개 선행 작업**: 아래 중 하나가 필요하다.
> 1. Decision 011과 2026-08-30 인앱 지도 결정을 새 3단계로 어떻게 편입/대체할지
>    (예: 할인 예매하기는 그대로 최우선 유지하고 그 다음에 예약→상세→네이버
>    순서로 끼워 넣는지, 길찾기 버튼은 아예 없애는지 등) 명시한 신규/개정
>    Decision을 `project/decision-log.md`에 기록, 또는
> 2. 이 3단계가 기존 CTA 버튼을 대체하는 게 아니라 **완전히 별도의 추가
>    버튼/영역**(예: 상세 정보 리스트의 "홈페이지" 행 자리를 3단계로 확장하고,
>    기존 CTA 버튼은 손대지 않음)이라는 점을 명확히 하는 지시 보완.
>
> 검색어 조합 규칙(제목 축약+장소 결합, 두 예시 포함)은 그대로 유효하니
> 재개 시 바로 활용 가능.

현재 우리 서비스의 **이벤트 카드 및 상세 화면**에서 유저에게 제공하는 외부 링크 버튼(예약, 상세 정보 등)의 노출 로직을 체계적으로 구현하고자 합니다. 

원천데이터(공공데이터 등)의 특성에 맞춰 아래의 **3단계 우선순위(Hierarchy) 로직**을 적용하여 데이터 스키마와 프론트엔드 렌더링 코드를 작성해 주세요.

## 📋 이벤트 링크 버튼 노출 3단계 우선순위

1. **1순위: 직접 예약/신청 링크가 있는 경우**
   - **조건:** 원천데이터에 개별 프로그램의 공식 접수/예약 URL이 포함되어 있는 경우. (ex. seoul_public_reservation 등)
   - **버튼 UI:** `[예약 / 신청하기]`
   - **동작:** 해당 원천 예약 URL로 다이렉트 이동.

2. **2순위: 예약 링크는 없지만, 공식 상세/홈페이지 URL이 있는 경우**
   - **조건:** 별도의 예약 시스템은 아니지만, 해당 행사의 공식 안내 페이지나 상세 소개 URL(`DETAIL_URL` 등)이 원천데이터에 포함된 경우. (각 원천데이터를 확인하여 상세 URL 이 있는지 확인할것.. 이건 Image URL과 다름)
   - **버튼 UI:** `[상세 정보 보기]` (또는 `[공식 안내 페이지]`)
   - **동작:** 원천데이터에 포함된 공식 상세 페이지 URL로 이동하여 행사의 깊이 있는 정보(일정, 준비물 등) 제공.

3. **3순위: 둘 다 없는 경우 (Fallback)**
   - **조건:** 예약 링크와 상세 페이지 링크가 모두 누락된 경우.
   - **버튼 UI:** `[네이버에서 정보 찾아보기]`
   - **동작:** 네이버 스마트 검색 딥링크로 연결.
     - **검색어 조합 규칙:** `https://search.naver.com/search.naver?query={이벤트명}` 형태로 URL 인코딩하여 유저가 네이버에서 바로 관련 정보를 탐색할 수 있도록 방어. (이벤트명이[서초문화재단] 서리풀청년아트갤러리 전시 [Fictionally Alibi] 등 되어있을 경우 사족이 너무 많아 title로 검색하면 나오지 않음.. 서리풀청년아트갤러리 전시 서초와 같이 title 축약 + 장소로 하여 검색어 조합할 것.
     [도림생활문화센터(4분기)]모임방 (1시간 5,000원/추가 시간당 2,500원)의 경우 모임방 영등포 이렇게 하면 또 검색하면 나오지 않음.. 이경우는 도림생활문화센터 모임방 영등포 이런식으로 되어야됨.
     상기와 같이 규칙에 대하여 네이버 스마트 검색 결과로써 정보에 대한 정확도가 높을 것 같은 검색어로 조합하여 url 던져야됨

## 🛠️ 요청 사항
1. **DB 스키마 반영:** 이벤트 테이블에서 위 링크들을 처리하기 위해 필요한 컬럼 구조(예: `reservation_url`, `detail_url` 등)를 제안해 주세요.
2. **UI 렌더링 코드:** 프론트엔드(Next.js / React)에서 위 3가지 우선순위 조건에 따라 알맞은 버튼과 링크를 렌더링하는 컴포넌트(또는 함수) 코드를 작성해 주세요.

---

[x] [개선사항 2] 당신은 Next.js App Router, TypeScript, 그리고 Supabase를 활용한 웹 서비스 개발에 능숙한 시니어 풀스택 엔지니어입니다.
> **완료 (2026-09-16)**: 기존 정기 요일 규칙 인프라(operating_weekdays 등,
> 2026-09-12 구현)는 그대로 재사용하고, 새로 필요했던 "단발성 예외 날짜"만
> 신규 구현 — public_holidays(공휴일 참고 목록)/event_operating_exceptions
> (이벤트별 실제 적용 예외) 2개 테이블, computeOperatingDates() 순수 함수,
> 관리자 예외일 편집기(공휴일 칩 클릭 추가 + 직접 입력), 유저 상세 화면
> "📅 달력으로 보기" 월별 하이라이트 캘린더. 공휴일 자동 수집(SpcdeInfoService)은
> 이 프로젝트 API 키가 아직 미등록(HTTP 403 실측 확인)이라 의도적으로 보류,
> 수동 등록 CRUD로 대체(사유는 구현 기록 참고). 실제 이벤트로 전 구간(등록→
> 예외 추가→캘린더 반영→삭제) 실측 검증 완료. 상세:
> implementation/2026-09-16-operating-day-calendar.md

현재 우리 프로젝트에서 유저 상세 화면에 들어갈 **'실제 운영일 하이라이트 캘린더 뷰'**와 이를 뒷받침할 **'운영일 규칙(기간 + 요일 휴무 + 수동 공휴일 예외) 데이터 및 백엔드 로직'**을 구현하려고 합니다. (※ 세부 시간/분 단위 관리는 공수 관계로 완전히 배제합니다.)

아래 요구사항에 맞는 백엔드 로직/데이터 구조와 프론트엔드 캘린더 컴포넌트 코드를 작성해 주세요.

### [요구 사항]

1. **운영일 규칙 정의 및 판정 로직 (Backend / DB Logic):**
   - 이벤트/상품의 기본 기간 (`start_date`, `end_date`) 설정. 
   - 정기 휴무 요일 규칙 (예: 매주 월요일 휴무 등) 처리 구조. (관리자 화면에서 설정할 수 있도록 되어있음)
   - 관리자가 연간 예외 공휴일 등을 수동으로 관리할 수 있는 최소한의 테이블 구조(또는 예외 처리 방식) 제안. -> 해당 테이블로 공휴일 데이터 가져올 수 있는 원천 데이터 소스 수집도 필요.. 해당 소스 수집은 연1회면 되니 수동으로 관리자가 하도록 함.
   - 주어진 날짜가 실제 오픈하는 날인지 판정하는 유틸리티 함수/로직 작성. 

2. **유저 상세 화면: 실제 운영일 하이라이트 캘린더 (Frontend Calendar View):**
   - 유저가 상세 페이지에서 기간(`start_date` ~ `end_date`)만 글자로만 보는 것이 아니라, 기간에 대하여 누르거나 그 옆의 캘린더(달력) 컴포넌트를 눌러서 달력 UI를 통해 직관적으로 확인할 수 있게 함.
   - 전체 기간 중 정기 휴무일이나 예외 휴무일을 제외하고, **실제 오픈하는 날짜들만 달력에 포인트 색상으로 하이라이트** 처리.
   - 휴무일이나 기간 외 날짜는 비활성화(Disabled / Grey out)하여 유저의 헛걸음을 방지하는 UX 구현.

### [출력 요구]
- 정기 요일 규칙과 예외를 반영하여 달력에 칠할 활성화 날짜 배열을 계산하는 유틸리티/훅 함수 코드
- 위 로직을 연동하여 실제 운영일을 시각적으로 보여주는 React 캘린더 컴포넌트 (TailwindCSS 활용)


[x] [개선사항 3] 당신은 Next.js App Router, TypeScript, Supabase, 그리고 웹 크롤링(Playwright/Cheerio) 구현에 능숙한 시니어 풀스택 엔지니어입니다.
> **완료 (2026-09-16)**: raw_data의 ORG_LINK/HMPG_ADDR/SVCURL 등은 각 인제스트
> 어댑터가 이미 소스별 우선순위로 정규화해 events.source_url/open_spaces.info_url
> 컬럼에 저장해 두고 있어(실측 확인, 예: tour-api-festival은 homepage가 HTML로
> 감싸여 오는 것까지 별도 추출) 이 정규화 컬럼을 그대로 크롤링 대상으로 썼다
> (raw_data를 admin UI에서 재파싱하면 소스별 예외를 놓칠 위험). 가격 큐레이션
> 소스3(공식 홈페이지 크롤링)가 이미 쓰던 extractGenericPageText를 재사용해
> POST /api/admin/scrape-source-url 신규, raw-data-modal.tsx에 "🌐 원천 링크
> 페이지 크롤링/조회" 버튼(sourceUrl 있을 때만 조건부) + 결과 뷰어 모달 추가.
> [개선사항 1](제휴 상품 크롤링)과 달리 대상이 공공기관 사이트라 정책 충돌
> 없음. 실제 이벤트로 재현 검증 완료. 상세:
> implementation/2026-09-16-source-url-crawl-viewer.md

현재 우리 프로젝트의 관리자용 상세 팝업(Modal) 화면에서, 이벤트/상시공간의 원천 데이터(`raw_data` JSON)에 포함된 공식 외부 URL주소 컬럼 (`ORG_LINK`, `HMPG_ADDR`, `SVCURL` 등)을 활용해 **해당 페이지의 상세 내용을 크롤링하여 보여주는 기능**을 추가하려고 합니다.

아래 요구사항에 맞추어 백엔드 API와 프론트엔드 UI 컴포넌트 코드를 작성해 주세요.

### [요구 사항]

1. **UI 위치 및 버튼 배치:**
   - 관리자 상세 팝업 내에 이미 존재하는 **`[DTLCONT 정돈해서 보기]` 버튼과 정확히 동일한 위치(또는 바로 옆)**에 **[원천 링크 페이지 크롤링/조회]** 버튼을 배치합니다.
   - 단, 해당 아이템의 `raw_data` 내부에 크롤링할 수 있는 URL 필드(`ORG_LINK`, `HMPG_ADDR`, `SVCURL` 등 우선순위 적용)가 실제로 존재하는 경우에만 버튼이 활성화(Conditional Rendering)되도록 합니다.

2. **백엔드 크롤링 API (`/api/admin/scrape-source-url` 등):**
   - 프론트엔드로부터 대상 URL을 전달받습니다.
   - Playwright(또는 가벼운 사이트의 경우 Axios/Cheerio)를 사용하여 해당 외부 사이트에 접속 후, 핵심 본문 내용이나 상품/행사 설명 텍스트를 파싱하여 리턴합니다.
   - 공공기관 사이트나 마이리얼트립 등 동적 렌더링 환경을 고려하여 안정적으로 텍스트/데이터를 긁어오도록 예외 처리(타임아웃, 차단 대응 등)를 포함합니다.

3. **프론트엔드 연동 및 뷰어 UI:**
   - 버튼을 누르면 로딩 상태(Spinner)가 표시된 후, 크롤링해 온 원천 페이지의 상세 내용이 관리자 팝업 내에서 깔끔하게 읽어볼 수 있는 형태로 렌더링되도록 구현합니다.
   - 기존 `[DTLCONT 정돈해서 보기]`와 토글(Toggle) 방식으로 번갈아 보거나, 하단에 정돈된 아코디언/박스 형태로 노출되도록 UI 레이아웃 구성.

### [출력 요구]
- 외부 URL을 받아 본문 데이터를 긁어오는 백엔드 API Route 핸들러 코드
- 관리자 상세 팝업 내의 버튼 추가 및 크롤링 결과 뷰어 연동 React 컴포넌트 코드 (TailwindCSS 활용)

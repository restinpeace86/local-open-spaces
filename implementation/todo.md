## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---

### Task 목록
- [ ] **[Task 9-6-17] 이벤트픽 5대 카테고리 인라인 피딩 전환 & 모바일 뒤로가기 모달 이중이동 방지** 📱🎪
  - **세부 작업 지시**:
    1. **5대 카테고리 칩 클릭 시 인라인 피딩 처리**: ⏭️ **스킵 (보류)**
       - 메인 카드 하단 5대 카테고리(`체험/클래스`, `야외/자연`, `전시/박물관`, `공연/축제`, `키즈/액티비티`) 클릭 시 외부 도감 페이지로 라우팅 이동하지 않고, '테마별 행사'와 동일하게 이벤트픽 화면 내부에서 해당 카테고리 카드 피드가 즉시 변경/노출되도록 구조 일치화.
       - **① 상세 스킵 사유**: `docs/spec.md`(Decision 008에 의해 UI/데이터 정책 SSOT로 지정된 문서) 2.2절 항목 2가 "5대 카테고리 Quick 아이콘 그리드: 클릭 시 하단 `[🏷️ 카테고리]` 탭 연동 및 해당 카테고리 즉시 필터링"이라고 명시적으로 규정하고 있다 (`src/components/home/quick-category-grid.tsx`가 `/region?category=...`로 라우팅하는 현재 구현이 이 SSOT를 그대로 따른 것). 이번 Task가 요구하는 "인라인 피딩(라우팅 없이 이벤트픽 내부 피드 전환)"은 이 SSOT 규정과 정면으로 상충하며, CLAUDE.md 제3장 제2조(Spec 우선)·제4조(추측 금지), 제7장 제1조(Spec 없는 기능 추가 금지)·제6조(역할 침범 및 기획 변경 금지)에 따라 구현 AI가 임의로 Spec을 재해석/변경할 수 없다.
       - **② 선행되어야 할 작업**: 기획 AI가 `docs/spec.md` 2.2절 항목 2 문구를 "인라인 피딩" 방식으로 명시적으로 개정하거나, 새 Decision(예: Decision 013)으로 이 변경을 승인·기록해야 재개 가능. 그 전까지는 현재 라우팅 방식(`/region?category=...`)을 유지한다.
    2. **모바일 뒤로가기(PopState) 제어 모달 이탈 방지**: ✅ **완료**
       - 이벤트 상세 모달(카드 크게 보기/길찾기 레이어) 오픈 시 history state 연동 또는 `popstate` 이벤트 핸들러를 보완하여, 모바일 폰 물리/제스처 뒤로가기 실행 시 이전 페이지(지도 등)로 이동하지 않고 **상세 모달만 닫히도록** 라우트/모달 상태 제어 보완.
       - 구현: `src/hooks/use-modal-back-close.ts` 신규 훅 — 모달 오픈 시 `history.pushState`로 더미 state를 쌓고 `popstate` 발생 시 `onClose` 호출, 다른 방식(X/배경 클릭)으로 닫힐 때는 `history.back()`으로 더미 state를 회수. `DetailModal`(`src/components/map/detail-modal.tsx`)과 `MapPreviewModal`(`src/components/map/map-preview-modal.tsx`, "🔍 크게보기" 풀스크린 지도 레이어)에 각각 독립 적용해, 두 레이어가 겹쳐 열려도 뒤로가기 1회당 최상단 레이어 1개씩만 닫히도록 처리.
       - 이 부분은 기존 Spec/Decision과 상충하지 않는 순수 UX 버그 수정(제2장 제1조 사용자 중심·제3조 멀티 디바이스 무결성 부합)이라 스킵 대상이 아니며 정상 구현·완료함.

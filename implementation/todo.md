## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---
# To-Do List

[개선사항 1] 관리자 워크벤치(중복 스팟 검수 및 매핑에서 데이터 적재 확인 불가

[현상]
- 캠핑장 중복 스팟 검수 화면에서 약 1,500건 이상에 대해 대표 상호명을 지정하고 그룹핑/병합 저장을 수행함.
- 하지만 작업을 마치고 페이지를 리프레시(새로고침)하자, 이전에 검수하고 저장했던 내용들에 대하여 다시 수행하라고 초기 상태로 돌아감 (작업 내용 유실 또는 미저장이거나 저장한 내용에 대한 체크가 없어서 저장 수행했음에도 또 올라오는 현상).

[확인이 필요한 핵심 포인트]
1. 백엔드 저장 로직 (API): 
   - 검수 완료 시(대표 상호명 저장 및 중복 그룹핑 확정) DB에 업데이트/인서트 쿼리가 정상적으로 실행되고 트랜잭션 커밋(Commit)이 제대로 이루어지고 있는지 확인해주세요.
   - 요청(Request)으로 넘어오는 데이터가 DB 모델에 올바르게 매핑되어 저장되고 있는지 파라미터 체크 부탁드립니다.

2. 조회 로직 (쿼리/필터링):
   - 데이터는 DB에 정상적으로 저장되었으나, 목록을 불러올 때 '검수 완료된 항목'이 제외되거나 잘못된 상태값(Status) 필터링에 걸려서 화면에 안 나타나는 것은 아닌지 확인해주세요.

3. 프론트엔드 상태 처리:
   - 저장 버튼 클릭 시 API 응답(Response)이 성공(200 OK 등)으로 떨어졌음에도 불구하고, 프론트엔드에서 상태 동기화나 라우팅 처리에 문제가 있었는지 점검해주세요.

관련 코드(API 핸들러, DB 모델, 프론트엔드 저장 함수 등)를 함께 점검하고 어느 부분이 누락되었거나 잘못되었는지 원인과 수정 코드를 제안해주세요.

**[✅ 완료, 2026-09-09]** — [[2026-09-09-dedup-scan-excludes-grouped-rows]]
실측으로 세 가설을 하나씩 검증: 저장은 정상(open_spaces.group_id 592건,
spot_dedup_groups 이력 265건 모두 캠핑장 중분류로 정확히 저장돼 있었음,
①번 기각) / 조회 쿼리에 실제 버그 있었음(`find_spot_dedup_candidates`가
`group_id`를 전혀 확인하지 않아 이미 처리된 행이 매 스캔마다 다시 후보로
잡힘, ②번 확정) / 프런트엔드는 문제 없었음(③번 기각). `and group_id is
null` 조건 추가로 수정, 실측(캠핑장 첫 페이지 50건 중 이미 그룹핑된 행이
10건→0건)으로 확인.

[개선사항2] 관리자 워크벤치 및 스팟 관리 화면(open_spaces 등)의 데이터 구조와 조회 로직을 개선하고자 합니다. 다음 요건에 맞춰 백엔드 API 및 쿼리 구조를 점검하고 수정해주세요.

[요구사항 요약]
1. 단독 데이터와 병합(중복 제거) 데이터의 화면 단일화:
   - 중복이 없는 '단독 스팟'은 원본 1개가 그대로 관리자 화면에 1개의 레코드로 노출되어야 합니다.
   - 중복이 있는 스팟들은 운영자가 검수 단계에서 병합(대표 상호명 지정)을 수행하면, 대표 레코드 1개만 남고 나머지 중복 원천 데이터들은 그 아래로 흡수(소속)되어야 합니다.
   - 결과적으로 **관리자 검수/큐레이션 화면에서는 단독 스팟이든 병합된 스팟이든 무조건 '하나의 깔끔한 레코드(Row)'로 동일하게 노출**되어야 합니다.
   - 다만 어떻게 병합되었는지 원본에 대한 정보는 어떤 방식이든 확인할 수 있어야합니다.
   - 또한, 그룹핑된 데이터중에 예약일자 서비스일자가 있는 데이터들은 추후 활용될 수 있습니다. 스팟픽>>마커 클릭>> 표준상호명과 정보 그 하위에 예약 가능한 리스트형태...로 단계별 진입을 통하여 예약하기까지 들어갈 수 있어야 합니다.
   - 
2. 후속 큐레이션(스팟 정보, 블로그 뱃지 등) 연동:
   - 중복이 1개로 합쳐진 대표 레코드를 기준으로, 메뉴/가격(스팟 큐레이션)과 블로그 뱃지(블로그 큐레이션) 작업이 **딱 한 번씩만 수행**될 수 있도록 데이터 구조가 연결되어야 합니다.

[체크해야 할 포인트]
- DB 스키마 및 마커/참조 구조 (마스터-차일드 혹은 상태 플래그 방식 등)
- 관리자 화면에 스팟 목록을 뿌려주는 조회 쿼리(API)에서 병합된 항목들이 어떻게 단일 레코드로 묶여서 내려오게 할 것인지에 대한 로직

위 내용을 반영하기 위해 현재 DB 모델, API 쿼리, 그리고 프론트엔드 연동 부에서 수정해야 할 부분이 있다면 코드와 함께 제안해주세요.

**[✅ 완료, 2026-09-09]** — [[2026-09-09-admin-grid-representative-collapse]]
`open_spaces.is_dedup_representative` 컬럼 신설(그룹 확정 시점에 대표를 딱
한 번 결정해 고정 — 기존 Step 78의 "매 호출 즉석 계산" 방식은 반경 조회가
사용자 위치에 따라 다른 물리적 행을 대표로 뽑을 수 있어 큐레이션이 소비자에게
전달 안 될 위험이 있었음, 근본 수정). `/api/admin/data-grid`(관리자 그리드 +
노출 중분류 대량 매핑 RowPicker + 모바일 큐레이션 워크벤치 큐가 모두 공유하는
단일 진입점)에 대표만 반환하는 필터 추가로 "단독/병합 무관 하나의 레코드"
요구사항 충족. `raw-data-modal.tsx`에 "🔗 병합된 원본 데이터 보기"로 원본
확인 가능. 대표 행의 소비자 노출 name도 원본 대신 표준상호명으로 교체(실측
확인). "예약 가능한 리스트 단계별 진입"은 Step 78에서 이미 구현 완료돼 있어
추가 변경 불필요.

[개선사항3] 공공데이터 원천소스 (localdata_playground) API(getPfctInfo3)의 설치장소 코드를 기준으로, 대분류 '기타' 하위의 표준 중분류 체계를 정비하고 흩어진 데이터를 일괄 이관하는 일회성 마이그레이션(데이터 정제) 작업을 진행하고자 합니다. 
(https://apis.data.go.kr/1741000/pfc3/getPfctInfo3)
설치장소코드 
A001:목욕장업소 | A002:도로휴게시설 | A003:도시공원 | A004:식품접객업소 | A005:아동복지시설 | A006:어린이집 | A007:유치원 |
A008:대규모점포 | A009:의료기관 | A010:주택단지 | A011:학교 |A012:학원 | A013:놀이제공영업소 | A020:주상복합 | A022:박물관 |
A023:종교시설 | A030:자연휴양림 | A031:하천 | A032:야영장 | A033:공공도서관 | A092:육아종합지원센터 | A093:유아교육진흥원

다음 요구사항에 맞춰 백엔드 데이터 마이그레이션 스크립트(또는 SQL 쿼리)와 처리 로직을 작성해주세요.

[마이그레이션 핵심 요구사항]

1. 원천 코드 기준 정렬 (Source of Truth):
   - 공공데이터 원천의 설치장소 코드(A001~A093 등)를 기준으로, 메인 13개 핵심 카테고리에 속하지 않는 나머지 데이터들은 대분류 '기타' 하위의 표준 중분류로 일원화해야 합니다.
   - 현재 여기저기 수동으로 옮겨져서 중구난방으로 흩어진 데이터들을 원천 코드 매칭을 통해 올바른 '기타' 하위 중분류 위치로 싹 모아야 합니다.

2. A004 (식품접객업소) 특별 예외 처리 규칙 (가장 중요):
   - 이미 수작업/큐레이션을 거쳐서 고정(Fix)된 **'놀이방식당' 중분류에 속해 있는 데이터들은 절대 건드리거나 덮어쓰지 말고 그대로 유지(EXCLUDE)**해야 합니다.
   - 위에서 제외된 나머지 흩어져 있던 'A004' 원천 데이터들만 싹 긁어모아서 대분류 '기타' 하위의 **'식품접객업소'** 표준중분류로 일괄 이관 및 집결시켜주세요.

3. 기타 코드들 (A092, A093, A001~A033 중 비핵심 코드 등):
   - 이미 중분류가 존재하더라도 대분류 '기타'에 속해있지 않거나 흩어져 있는 경우, 대분류를 '기타'로 맞추고 해당 코드별 표준 중분류로 데이터를 일괄 이동/정렬해주세요.
   
[출력 요청 사항]
- 위 조건(특히 A004의 '놀이방식당' 데이터 보호 및 나머지 A004의 '식품접객업소' 집결)을 안전하게 수행할 수 있는 마이그레이션 쿼리(또는 파이썬 스크립트 로직)를 제안해주세요.




하기 설치장소 코드들에 대하여 설치장소 코드별로 표준중분류가 없는것들에 대하여 확인 후 대분류 '기타'항목에 신규로 중분류 만들고 거기에 데이터를 이관할 것 (일회성).
단, A004 항목은 표준중분류 :'놀이방식당'에 데이터가 포함되지 않은 데이터들에 한해서 식품접객업소 표준중분류 추가 및 해당중분류에 다 모아넣기.
A092나 A093등의 중분류는 이미 존재하지만 대분류 '기타'에 존재하지 않으면 '기타'쪽으로 옮길 것


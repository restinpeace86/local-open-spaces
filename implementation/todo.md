## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---

## 진행 상태 (Harness 자율 실행 — 2026-09-11 순차 처리)
- [x] 개선사항1 검토: 맘스픽 메인엔 이미 상단 장소 선택 바/검색창이 없음(확인 완료, 제거할 대상 없음).
      Gating은 Step 96·107에서 이미 구현 — guest 클릭 시 로그인 안내 팝업(LoginPromptModal, 이 앱에
      별도 "로그인 화면" 페이지가 없어 소셜 로그인 버튼이 포함된 안내 모달이 그 역할을 겸함),
      not_sprout_yet 클릭 시 첫 글 쓰기 유도(SaessakMomGuideModal), 정상 유저는 제약 없음 — 스펙과
      일치함을 재확인. 추가 코드 변경 없음.
- [x] 개선사항2 (Step 108): **긴급 DB 버그 수정** — `get_nearby_spaces_and_events` RPC가
      Step 82(2026-09-09) 이후 `coalesce(standard_name, name)` 타입 불일치(text vs varchar,
      PL/pgSQL RETURN QUERY는 암묵 캐스팅 안 함)로 SPACE를 포함한 모든 호출에서 100% 실패 중이었음
      (스팟픽 기본 지도·홈 피드·맘스픽 인기 스팟 등 전역 영향). `::character varying` 캐스팅 추가로
      수정, 프로덕션 DB 직접 호출로 검증 완료. (완료: 2026-09-11)
- [x] 개선사항3 (Step 109): "전체보기" 바텀시트 4곳(이벤트픽 대/중분류 선택=
      MajorCategoryGrid, "지금 이 순간 함께하기 좋은 알찬 픽"/"놓치면 후회하는 인기
      만점 예약 픽"/"오늘 전체보기"=EventBrowseSheet)의 이미지 포함 그리드(EventCard/
      FeedCard)를 이미지 없는 1열 리스트(`EventListRow` 신규, 4단 라인: 제목+거리 /
      뱃지 / 행사·운영기간 / 예약기간)로 통일. EventCard/FeedCard 자체는 다른 화면
      (슬라이더 등)에서 여전히 이미지가 필요해 그대로 유지, 새 전용 컴포넌트만 추가.
      (완료: 2026-09-11, 상세: implementation/2026-09-11-eventpick-browse-sheets-image-free-list.md)
- [x] 개선사항4 (Step 110): MajorCategoryGrid — 대분류 클릭 시 첫 번째(왼쪽) 중분류
      자동 선택(onSelectMin 즉시 호출, 0건 중분류는 건너뜀)으로 결과 로딩이 바로
      시작되도록 개선. 두 바텀시트(MajorCategoryGrid/EventBrowseSheet) 모두 로딩
      스켈레톤을 신규 `EventListSkeleton`(1열 리스트 모양)으로 통일. 중복 fetch 점검
      결과 실제 중복 호출 지점 없음(코드 변경 없이 점검 결과만 기록).
      (완료: 2026-09-11, 상세: implementation/2026-09-11-eventpick-browse-sheets-autofocus-and-skeleton.md)
- [x] 개선사항5 (Step 111): "전체보기" 3곳(오늘/알찬 픽/예약 픽)에 스팟픽과 동일한
      도(道) 단위 1차 필터(province.ts 재사용) + 거리순 정렬(sortByDistanceIfKnown
      재사용) 적용. 신규 getTodayEventsPage(오늘 전체보기 전용, Hero 미리보기용
      getTodayEvents는 그대로 유지) + getCurrentlyOngoingEventsPage/
      getReservationOpenEventsPage를 fetchAllRowsChunked 기반으로 재작성(PostgREST
      max_rows=1000이 프로덕션에도 적용됨을 실측 확인 — 단일 range() 요청은 최대
      1,000건까지만 반환됨). EventBrowseSheet에 userLocation prop 추가로 실제 GPS
      좌표를 서버에 전달. MajorCategoryGrid(4번째, "검토" 대상)는 이미 distance 정렬
      중이라 이번엔 현황만 기록, 추가 변경 없음.
      (완료: 2026-09-11, 상세: implementation/2026-09-11-eventpick-browse-sheets-distance-sort.md)
- [x] 개선사항6 (Step 112): 이벤트 상세카드를 8단 구조(이미지 슬라이드/뱃지(카테고리+
      진행상태)/제목+인터랙티브 거리/행사기간/예약기간/설명 토글/미니맵/하단 고정
      CTA)로 재설계. 스팟(SPACE) 분기는 완전히 그대로 유지, 기존 부가 정보(연령대상/
      예약 안내)는 삭제하지 않고 새 구조 안에 재배치.
      (완료: 2026-09-11, 상세: implementation/2026-09-11-event-detail-card-8-section-restructure.md)
- [x] 개선사항7-2 + 8 (Step 113): 이벤트픽 블로그 큐레이션 관리자→DB→유저 파이프라인.
      `events.curated_blog_urls text[]`(최대 3개) 컬럼 신규, 관리자 Events 탭 상세
      팝업에 "블로그 큐레이션" 버튼(검색→체크박스 선택→저장), 유저 이벤트 상세
      화면(8단 구조 6단 아래·7단 위)에 "방문 후기/추천 블로그" 카드 조건부 노출(0건
      시 섹션 숨김). open_spaces의 기존 블로그 검색/본문 API(Decision 021)를
      그대로 재사용.
      (완료: 2026-09-11, 상세: implementation/2026-09-11-event-blog-curation-admin-and-user.md)
- [x] 개선사항7-1 (Step 115): 가격 정보 파싱 고도화. 4개 이벤트 소스를 전부 실측
      조사한 결과 2곳(SEOUL_CULTURE_EVENTS의 USE_FEE, GG_CULTURE_EVENTS API1의
      PARTCPT_EXPN_INFO)은 이미 원본 API가 가격을 자유 텍스트로 제공해 크롤링
      불필요, 나머지(GG_CULTURE_EVENTS API2/SEOUL_YEYAK)는 설명 텍스트에서 라벨+
      금액 패턴을 파싱(`price-parser.mjs` 신규). SEOUL_YEYAK의 실제 원천 URL을
      직접 fetch해 확인한 결과 가격이 정적 HTML에 없어(동적 로딩 추정) 헤드리스
      브라우저 없이는 크롤링이 실제로 불가능함을 실측 확인 — 안 되는 걸 구현하는
      대신 정직하게 null 처리. `events.price_text`/`source_url` 컬럼 신규, 유저
      상세 화면(가격/홈페이지 행)에 노출.
      (완료: 2026-09-11, 상세: implementation/2026-09-11-price-parsing-and-seoul-yeyak-reroute.md)
- [x] 개선사항9 (Step 115, Decision 024로 재승인 후 구현): 최초 스킵(Decision 017과
      충돌) 후 사용자가 "이거는 open_spaces말고.. 다시 이벤트 테이블로"라고 명시적
      재확인 → **Decision 024**로 Decision 017 2항 개정. SEOUL_YEYAK의 체육시설/
      공간시설을 open_spaces → events로 재라우팅(UI 카테고리 오매핑/키즈 뱃지 좁은
      판별 등 Decision 017 9항 취지는 그대로 유지). 기존 잘못 적재된 1,525건을
      raw_data로 재구성해 events로 이관 + 원본 삭제 완료(실측 검증됨).
      (완료: 2026-09-11, 상세: implementation/2026-09-11-price-parsing-and-seoul-yeyak-reroute.md)
- [x] 개선사항10 (Step 114): Event↔Spot 양방향 링킹. `events.space_id`(기존 FK
      컬럼, 신규 마이그레이션 불필요)를 재사용 — 프로덕션에서 좌표 30m 이내 +
      이름 부분일치 기준 배치 매칭으로 11,977건 자동 연결. 관리자 Events 탭
      상세 팝업에 "연결된 스팟" 편집기(SpotPicker 재사용) 추가. 유저 화면:
      이벤트 상세에 "📍 연결된 장소" 탭 가능 행, 스팟 상세에 "🎪 진행 중인
      이벤트" 목록(0건 시 숨김) — 둘 다 탭하면 DetailModal이 자기 자신을
      중첩 렌더링해 그 상세를 연다(부모 화면 15곳+ 수정 없이 자기완결적으로 구현).
      (완료: 2026-09-11, 상세: implementation/2026-09-11-event-spot-bidirectional-linking.md)
- [x] 개선사항7-2 후속 (Step 116, 사용자 추가 지시): "events쪽의 블로그 큐레이션에도
      가격이나 연령과 관련된 단어들을.. 노란색 형광펜 색칠해줘.. 블로그의 가독성을
      위하여 줄바꿈도.." — `highlightKeywordsOnly` 신규(카테고리 뱃지 없이 명시적
      키워드/패턴만 하이라이트), 가격 라벨/금액 키워드 세트 추가. `naver-blog-body.ts`의
      본문 추출이 `structuredText`의 문단 줄바꿈(`\n`)을 공백으로 뭉개던 버그 수정 +
      `whitespace-pre-line` 렌더링 적용.
      (완료: 2026-09-11, 상세: implementation/2026-09-11-event-blog-curation-highlighting-and-readability.md)
- [x] 개선사항7-2 후속2 (Step 117, 사용자 추가 지시): "가격 무료인지.. 유료면 가격에
      대하여 블로그글 복붙하면 파싱할 수 있는거.. 적을 수 있는거 줘야지.. 타겟 연령..
      선택체크 할 수 있도록.. 아이/분수/빛/불꽃 등 키워드도 색칠해줘" — 기존
      `/api/admin/events/blog-curation` PUT을 확장해 `price_text`(자유 텍스트 직접
      입력 + "현재 블로그에서 자동 채우기" 버튼, `parse-price-from-text.ts` 신규:
      scripts/의 price-parser.mjs와 동일 로직을 TS로 중복 구현 — scripts/↔src/
      비상호참조 컨벤션)와 `target_audience`(EVENT_PICK_TARGET_AUDIENCES 4개 중
      단일 선택 토글 버튼, 기존 데이터그리드의 11개 값 TargetAudienceEditor와는
      별개의 좁은 목적)를 한 번에 저장. "건드리지 않은 필드는 PUT body에서 아예
      뺀다"(현재값==최초로드값이면 키 생략) 안전장치로 다른 화면에서 이미 검수한
      값을 실수로 덮어쓰지 않게 함. `KID_APPEAL_HINT_KEYWORDS` 신규(아이/분수/
      불꽃놀이/동화/공연/인형극/마술쇼/캐릭터/야경 등 + "아이들과 가기 좋은" 류
      문구) — "원" 같은 단일 글자·오탐 위험 단어는 제외.
      (완료: 2026-09-11, 상세: implementation/2026-09-11-event-curation-price-target-audience-kid-keywords.md)
- [x] 개선사항10 후속 (Step 118, 사용자 추가 지시): "이벤트픽에 스팟 연결하면 연결됨이라고
      뜨는데.. 해당 장소가 노출 중분류가 있는지 확인하고.. 없으면 노출 중분류를
      입력하라고 하고 해당 팝업화면에서 선택 및 저장 가능하도록 해줘" — 실측 확인
      결과 노출 중분류(service_category_id)가 없어도 필터 없는 기본 스팟픽 지도엔
      나오지만(location_precision=EXACT만 요구), 카테고리 필터(get_spots_by_service_
      category RPC)로는 전혀 걸리지 않음. `/api/admin/data-grid/space-link`에 GET
      추가(연결된 open_spaces의 name/service_category_id 조회), `SpaceLinkEditor`가
      스팟 연결/로드 시마다 이를 조회해 있으면 초록 배지, 없으면 경고 + 기존
      `ServiceCategoryEditor`와 동일한 선택 UI(저장은 기존 bulk-category-mapping
      POST 재사용)를 그 자리에서 보여준다.
      (완료: 2026-09-12, 상세: implementation/2026-09-12-space-link-service-category-check.md)
- [x] 개선사항9 후속 (Step 119, 사용자 추가 지시): "시간관련 필드.. 예약 일자들도 항상
      최신값으로 갱신에 포함하는게 맞을거같은데... 주기적으로 바뀌어야 하는 필드에
      대하여 좀더 확인하고.. 최신값으로 갱신해" — 서울형 키즈카페 실측 조사 중 발견한
      버그: `upsertRowsSafeMerge`의 "기존 값 있으면 보존" 규칙이 events.start_date/
      end_date/reservation_start_date/reservation_end_date/is_active/booking_status
      처럼 회차가 바뀔 때마다 실제로 달라지는 필드에도 적용돼, 한 번 채워진 뒤로는
      새 예약 회차가 열려도 옛날 값에 영원히 고정되는 문제가 있었음(관악구 난곡동점 등
      대부분의 지점이 2026-08-25~09-07 회차에 고정, 실제 원본은 이미 09-10~09-21로
      넘어감). `ALWAYS_REFRESH_FIELDS` 신규 도입해 이 6개 필드만 예외적으로 "최신
      incoming이 항상 이김"으로 뒤집었다(category_min/target_audience 같은 관리자
      수동 분류 필드는 절대 포함하지 않음 — 회귀 방지 테스트로 확인). SEOUL_YEYAK
      1회 수동 재수집으로 기존 데이터도 즉시 반영.
      (완료: 2026-09-12, 상세: implementation/2026-09-12-safe-merge-always-refresh-time-fields.md)
- [x] 개선사항3 후속 (Step 120, 사용자 버그 신고): "이벤트픽 전체보기 바텀시트에서
      리스트 누르면 상세카드 뜨는데, 취소하면 바로 전단계 리스트로 안 돌아가고
      그냥 사라져버리고 이벤트픽 화면이 뜬다.. 다시 전체보기부터 눌러야 함" — 원인:
      EventBrowseSheet(오늘 전체보기/알찬 픽/예약 픽 3곳)의 onSelectItem이 상세카드를
      열면서 동시에 시트 자체를 닫아버렸음(MajorCategoryGrid는 자체 로컬 상태라
      원래 문제 없었음). 시트를 닫지 않고 상세카드만 위에 겹쳐 띄우도록 수정 +
      "방금 누른 항목 포커스 표시"(파란 테두리 강조 + scrollIntoView) 신규 추가,
      전체보기 4곳 모두 일관 적용.
      (완료: 2026-09-12, 상세: implementation/2026-09-12-eventpick-browse-sheet-detail-return-focus.md)
- [x] 개선사항10(맘스픽) 후속 (Step 121, 사용자 버그 신고): "맘스픽 들어가면 장소선택이
      같이 보여.. 첫글은 안쓴상태면 맘스픽 내용만 보여야돼.. 액션하면 그때 팝업떠서
      권한없다고 첫글쓰러 가자고 해야" — not_sprout_yet(로그인 O, 첫 글 X)이 guest와
      달리 실제 글쓰기 폼(SurveyReviewComposer, 1단계 장소선택)을 항상 그대로
      렌더링하고 있던 것을 guest와 동일한 소프트월 버튼으로 통일. 안내 팝업의
      "첫 글 쓰러 가기"를 눌러야만 폼이 드러난다(isComposerRevealed 신규).
      (완료: 2026-09-12, 상세: implementation/2026-09-12-mompick-first-post-softwall-unify.md)
- [x] 개선사항10 후속2 (Step 122, 사용자 추가 지시): "이미 등록한적 있는데 일자만
      바뀌는 이벤트들(난지캠핑장/서울형 키즈카페 등).. 8월꺼에 스팟연결+노출
      중분류 해놨는데 9월꺼 가져왔으면 자동 매핑 가능한가?" — external_id가
      매달 새로 발급되는 소스(안내글마다 새 SVCID)는 새 이벤트 행이 매번 space_id
      NULL로 시작해 관리자가 매번 재연결해야 했음. Step 114의 일회성 매칭 SQL
      (좌표 30m+이름 부분일치, 멱등)을 `match_events_to_open_spaces()` RPC로 옮겨
      매일 배치 후처리 단계에 상시 편입. 노출 중분류는 스팟(open_spaces) 쪽 값이라
      space_id만 연결되면 자동으로 함께 적용됨. 프로덕션 1회 수동 실행으로 기존
      미연결 331건 즉시 정리(한성백제박물관점 9월 안내글 포함 확인).
      (완료: 2026-09-12, 상세: implementation/2026-09-12-auto-match-events-to-open-spaces-daily.md)
- [x] 개선사항2 후속 (Step 123, 사용자 지시): "updated_at 어 이거 추가해.. 자동
      갱신 트리거도 하고" — "오늘 등록건"(created_at)만으로는 external_id가
      안정적인 소스가 오늘 실제로 갱신(예약 회차 변경 등)됐어도 절대 안 잡히는
      사각지대를 확인한 직후의 후속 요청. `events.updated_at` 컬럼 + 범용 트리거
      함수(`set_updated_at()`) 신규 — raw_data 등 잡음 컬럼은 비교에서 제외해
      "값이 하나도 안 바뀐 재적재"는 갱신으로 잡히지 않게 함(프로덕션 실측 검증
      완료). 요약 카드에 "오늘 갱신" 건수 추가, events 탭에 "수정일(updated_at)"
      필터 섹션(오늘 갱신건 보기/최근 3일) 신규 — 등록일 필터와 동시에 걸리면
      서로 좁혀버리는 함정을 막기 위해 한쪽을 켜면 다른 쪽을 항상 비운다.
      (완료: 2026-09-12, 상세: implementation/2026-09-12-events-updated-at-column-and-filter.md)
- [x] 개선사항2 후속2 (Step 124, 사용자 버그 신고 + 검토 요청): "표준 중분류가
      미지정NULL 체크 된거는 활성 상태가 is_active true로 된게 안먹던데? 아주
      옛날 이벤트들도 다 나오고 있어.. 비활성 데이터 backup 테이블로 옮기는건
      어때? 검토해봐" — 실측 결과 필터 로직 버그가 아니라 데이터 정합성 버그였음:
      `deactivateExpiredEvents()`가 `.update().select('id')` 1회 호출만 해 PostgREST
      max_rows(1000)에 걸려 매일 최대 1000건만 비활성화되고 나머지는 누적, 실측
      시점 백로그 21,453건(전체의 75%, 2016년 종료건도 is_active=true로 방치).
      "id 소량 SELECT → 그 id로 UPDATE"를 0건까지 반복하는 방식으로 수정 +
      프로덕션 즉시 실행으로 백로그 전량 정리(잔여 0건 확인, active 24,789→3,336).
      백업 테이블 이전은 검토 결과 비권장(events 전체가 28,698건뿐이라 성능
      이점 미미, 오래된 건도 259~278건뿐 — 진짜 원인은 이 버그였음).
      (완료: 2026-09-12, 상세: implementation/2026-09-12-deactivate-expired-events-backlog-fix.md)

# To-Do List
[개선사항 1] 현재 '맘스픽' 메인 화면의 UI 구조와 접근 제어(Gating) 로직을 아래와 같이 전면 수정해 주세요.

### 📌 [요구사항] 맘스픽 메인 화면 및 권한별 인터랙션 플로우

1. **맘스픽 메인 화면 UI 정돈 (상단 장소 선택 제거)**
   - 맘스픽 메인 화면 진입 시, 상단에 장소 선택 바나 검색창 같은 불필요한 요소가 노출되지 않도록 제거해 주세요.
   - 오직 순수하게 맘스픽 콘텐츠 영역(**파워맘/우수맘 추천, 인기 우수글, 실시간 라이브 글 등**)만 깔끔하고 온전하게 렌더링되도록 뷰를 수정해 주세요.

2. **유저 상태별 접근 제어 (Gating & Interceptor) 로직 적용**
   - 유저가 맘스픽 메인 화면의 콘텐츠를 누르거나 특정 액션을 시도하는 시점에 아래와 같이 분기 처리(인터셉트)를 해주세요.
   - **① 비로그인 사용자:** 
     - 콘텐츠 터치 시, *"로그인이 필요합니다"* 안내 메시지 팝업 노출 후 **[로그인 화면]**으로 이동.
   - **② 로그인 완료 + 첫 글 미작성 사용자:** 
     - 메인 화면 자체는 배경으로 보이되, 화면 전면에 **투명 인터셉트 레이어**를 두거나 터치 시 가로채어 자연스럽게 **[첫 글 쓰기 화면]**으로 유도.
   - **③ 정상 유저 (로그인 + 첫 글 작성 완료):** 
     - 아무런 제약 없이 맘스픽 메인의 모든 콘텐츠와 기능 정상 이용 가능.

[개선사항 2] 글쓰기 화면 장소 선택 에러 버그 수정
현재 유저가 글쓰기 화면에서 장소를 검색할 때('어느 스팟인가요?'), UI에 다음과 같은 치명적인 DB 에러가 빨간색으로 노출되고 있습니다:
- 에러 메시지: `structure of query does not match function result type`

---

### 📌 [요구사항] 장소 검색 쿼리 및 DB 함수 스키마 정합성 맞추기

1. **원인 확인:**
   - 장소 검색 및 반경(Geo-query) / 거리 계산 / 데이터 조회 로직을 수정하는 과정에서, DB에 정의된 커스텀 함수(Function)의 **리턴 타입(Return Type / TABLE 정의)**과 내부의 실제 `SELECT` 쿼리문이 반환하는 컬럼(개수, 순서, 타입) 간의 불일치가 발생했습니다.

2. **조치 사항:**
   - 장소 검색 및 조회를 담당하는 SQL 쿼리문과 PostgreSQL 함수(Function) 정의부를 점검해 주세요.
   - `SELECT` 절에서 반환하는 컬럼(`id`, 상호명, 주소, 좌표, 거리 등)의 구조가 함수 생성 시 선언된 `RETURNS` 타입(또는 매핑 모델)과 **100% 일치**하도록 수정해 주세요.
   - 에러가 유저 화면에 그대로 노출되지 않도록 예외 처리(Try-Catch) 및 로그 처리도 함께 점검해 주세요.

---

[개선사항 3]   현재 이벤트픽 화면 서비스 내에서 제공되는 **모든 '전체보기' 바텀시트의 프리뷰 카드 목록**의 UI 구조와 성능을 일괄적으로 개선해 주세요. 특정 화면뿐만 아니라 아래 대상 섹션의 전체보기 바텀시트에 모두 동일하게 적용해 주세요.

### 📌 [요구사항] 전역 '전체보기' 바텀시트 프리뷰 카드 공통화 (1열 리스트 구조)

1. **적용 대상 (모든 전체보기 바텀시트)**
   - 이벤트픽 (대/중분류 선택 바텀시트)
   - "지금 이순간 함께하기 좋은 알찬 픽" 전체보기 바텀시트
   - "놓치면 후회하는 인기 만점 예약 픽" 전체보기 바텀시트
   - 메인 화면 Hero 카드 영역의 "오늘 전체보기" 바텀시트

2. **공통 프리뷰 카드 UI 스펙 (이미지 제거 및 1열 리스트)**
   - **레이아웃:** 기존의 2열 그리드 구조를 전면 폐기하고, **1열 리스트 형태(한 행에 하나의 카드)**로 공통 컴포넌트화해 주세요.
   - **이미지:** 카드별 이미지 렌더링 로직은 완전히 제거하여 대량 리스트 렌더링 시 버벅임(지연)이 없도록 성능을 최적화해 주세요.
   - **카드 내부 정보 배치 (4단 라인 구조 유지):**
     - **첫 번째 줄:** **제목** (상단 왼쪽, Bold) 및 **현재 위치와의 거리** (상단 오른쪽, 서브 텍스트)
     - **두 번째 줄:** **뱃지 영역** (카테고리 및 상태 뱃지)
     - **세 번째 줄:** **행사 / 운영기간** 
     - **네 번째 줄:** **예약기간**

3. **기대 효과**
   - 앱 전체의 바텀시트 리스트 UI 일관성 확보 및 이미지 로딩 부하 제거를 통한 매끄러운 스크롤/조회 성능 달성.

[개선사항 4] 현재 이벤트픽 '전체보기' 바텀시트 진입 시, 가장 왼쪽 중분류가 자동 포커스되면서 데이터를 불러오는 과정의 UX와 반응 속도를 아래와 같이 개선해 주세요.

---

### 📌 [요구사항] 중분류 자동 포커스 및 데이터 로딩 반응 속도 최적화

1. **중분류 탭 초기 포커스 및 즉각적인 UI 피드백**
   - 바텀시트가 열릴 때 가장 왼쪽 중분류 탭이 자동 선택되는데, 이 과정에서 **탭의 'Active(선택된 상태)' 디자인이 지연 없이 즉시 반영**되도록 탭 렌더링 우선순위를 높여주세요.

2. **데이터 로딩 중 스켈레톤 UI (Skeleton Loader) 적용**
   - 중분류 탭이 선택되거나 변경되어 새로운 데이터를 불러오는 동안, 리스트 영역에 **가벼운 스켈레톤 프리뷰 카드**를 노출하여 유저가 로딩 중임을 명확히 인지할 수 있게 해주세요.
   - 빈 화면이나 멈춘 듯한 느낌이 들지 않도록 시각적 피드백을 반드시 추가해 주세요.

3. **데이터 펫치(Fetch) 및 렌더링 성능 최적화**
   - 초기 진입이나 탭 전환 시 동일한 API가 중복 호출되거나 렌더링이 병목을 일으키는 원인이 있는지 점검하고, 데이터가 들어오는 대로 1열 리스트가 부드럽게 꽂히도록 최적화해 주세요.

---

[개선사항 5] 현재 이벤트픽 서비스 내 모든 '전체보기' 바텀시트 목록의 데이터 정렬 기준을 **'현재 내 위치 기준 거리 가까운 순'**으로 일괄 적용해 주세요. 기존의 섹션별 필터링 조건은 그대로 유지한 상태에서 정렬만 거리 순으로 변경해야 합니다.
추가 사항 - 지역 필터링: 유저의 현재 위치 기반(또는 수동 선택된) 도 단위(예: 서울/경기 등)로 대상 데이터 1차 필터링. (스팟픽과 동일한 구조)
거리 계산 및 정렬: 해당 도(Do) 내의 데이터들만 대상으로 현재 좌표 기준 거리 계산 ➡️ ORDER BY distance ASC 정렬.
페이지네이션: 정렬된 결과물 중 상위 N개씩 끊어서 프론트엔드로 전달 (무한 스크롤 연동).

### 📌 [요구사항] 전체보기 바텀시트 '거리 가까운 순(ASC)' 정렬 적용

1. **적용 대상 및 섹션별 필터링 조건 + 거리 정렬 규칙**
   - **① 메인 히어로 '오늘 전체보기':** 
     - 필터: 오늘 당일 진행되는 데이터만 추출
     - **정렬: 현재 위치 기준 거리 가까운 순 (ASC)**
   - **② "지금 이 순간 함께하기 좋은 알찬 픽" 전체보기:** 
     - 필터: 현재 운영 중인 기간 내에 오늘 날짜가 포함된 항목
     - **정렬: 현재 위치 기준 거리 가까운 순 (ASC)**
   - **③ "놓치면 후회하는 인기 만점 예약 픽" 전체보기:** 
     - 필터: 현재 예약 접수 기간 내에 오늘 날짜가 포함된 항목
     - **정렬: 현재 위치 기준 거리 가까운 순 (ASC)**
     - *(참고: 이벤트픽 대/중분류 선택 바텀시트 역시 동일하게 해당 중분류 기준 + 거리 가까운 순으로 정렬되도록 검토)*

2. **기술적 구현 참고 사항**
   - 유저의 현재 좌표(Latitude, Longitude)를 기반으로 Haversine 공식 또는 PostGIS 등을 활용하여 거리(`distance`)를 계산한 뒤, SQL 쿼리 또는 클라이언트 정렬 시 `ORDER BY distance ASC` 조건이 올바르게 반영되도록 구현해 주세요.
   - 페이지네이션(무한 스크롤) 시에도 거리순 정렬 누락이나 중복 데이터가 발생하지 않도록 쿼리 기준점을 탄탄하게 잡아주세요.
  
[개선사항 6] 이벤트 상세카드를 하기 구조에 맞춰서 재구성해줘

이벤트 상세 카드 8단 구조 스펙
1단: 최상단 이미지 영역

대표 이미지 슬라이드 (여러 장일 경우 좌우 넘겨보기 지원)
예외 처리: 이미지가 아예 없는 경우 해당 영역 통째로 숨김 처리(Gone)

2단: 뱃지 영역

카테고리 뱃지 및 현재 진행 상태 뱃지 노출

3단: 이벤트 명 & 거리 (인터랙티브)

좌측: 이벤트 명 (Bold 체로 강조)

우측: 현재 위치 기준 거리 (🧭 X.Xkm ›)

인터랙션: 우측 거리 영역 탭 시 곧바로 인앱 길찾기 API 실행

4단: 이벤트(운영) 기간

행사가 실제로 진행되고 운영되는 기간 명시

5단: 예약 기간

티켓팅 및 사전 접수가 열려 있는 기간 명시

6단: 상세 설명 (텍스트 토글)

기본 2줄 요약 노출 후 하단 [더보기] 버튼 누르면 전체 내용 확장, [접기] 누르면 다시 축소

7단: 인앱 지도 & 스팟 마커 (미니 맵)

이벤트 개최 장소의 위치를 시각적으로 확인시켜 주는 가벼운 지도 컴포넌트 배치

8단: 하단 고정 CTA 버튼

예약 상품인 경우: [예약하기] (외부 예약 페이지/서울공공예약 링크 연동)

예약이 없는 일반/공공 행사인 경우: [이 장소로 길찾기] 등 핵심 액션 버튼

---

[개선사항 7] 현재 개발 중인 이벤트픽 관련 데이터 파싱 로직과 관리자 화면에 아래의 기능들을 추가 및 고도화해 주세요.

### 📌 [요구사항] 가격 크롤링 Fallback 및 관리자 블로그 큐레이션 연동

1. **가격 정보 파싱 고도화 (URL 크롤링 Fallback)**
   - 이벤트 원천 데이터의 텍스트(Description) 설명에서 가격 정보를 우선적으로 파싱합니다.
   - 만약 무료가 아닌 유료 이벤트로 추정되나 설명에 가격이 없고, **원천 URL(`source_url`)이 존재하는 경우**, 해당 URL에 접근하여 가격 정보를 추출하는 크롤링 Fallback 로직을 구현해 주세요.
   - 없으면 가격정보에 대하여 null로 비워둘것.. 가격에 대하여 15000원 이런식으로 되어있을 수 있고 성인 15000원 어린이 10000원 이런식으로 되어있을 수도 있음.. DB에 데이터는 유연하게 적재할 수 있도록 하는 것이 필요.

2. **관리자 화면 이벤트 상세 팝업 내 '블로그 큐레이션' 기능 추가**
   - 관리자 화면의 `Events` 탭에 있는 개별 이벤트 항목의 **[상세 팝업]** 내부에 **[블로그 큐레이션]** 버튼을 추가해 주세요.
   - 관리자가 해당 버튼을 통해 특정 이벤트와 연관된 양질의 블로그 후기/안내 링크를 검색하고 등록·수정·삭제할 수 있는 관리 UI 및 DB 연동 파이프라인을 구축해 주세요.
  
[개선사항 8] 
유저용 이벤트 상세 화면 내 '큐레이션 블로그' 연동 구조
데이터 파이프라인 (Admin ➡️ DB ➡️ User)

관리자 화면에서 블로그 후보 3개 중 체크하여 저장한 URL들이 해당 이벤트 데이터의 필드(예: curated_blog_urls)에 안전하게 저장됩니다.

유저 상세 화면에서의 노출 (Conditional Rendering)

위치: 6단 상세 설명 아래쪽, 또는 지도 미니맵 위쪽에 [방문 후기 / 추천 블로그] 형태의 깔끔한 카드 리스트로 배치합니다.

조건부 노출: 만약 관리자가 저장한 블로그 URL이 단 1개도 없다면 해당 섹션 전체를 통째로 숨김(Gone) 처리하여 빈 화면이 뜨지 않도록 깔끔하게 방어합니다.

유저 인터랙션

유저가 상세 페이지를 보다가 큐레이션된 블로그 카드를 탭하면, 인앱 브라우저(또는 외부 브라우저)로 부드럽게 연결되어 생생한 현장 후기나 사진을 바로 확인할 수 있습니다.

---

[개선사항 9] 
현재 데이터 파이프라인에서 상시성 스팟(`open_spaces`)과 시한성 이벤트(`events`)의 원천 데이터 수집 및 라우팅 로직을 이분법적으로 명확하게 분리하고, 기존에 섞이거나 크로스되던 모호한 로직을 깔끔하게 정리(Refactoring)해 주세요.

### 📌 핵심 요구사항

1. **엄격한 이분법적 수집 라우터 (Ingestion Router) 구축**
   - 원천 데이터의 속성(운영 기간 유무 등)을 기준으로 수집 단계에서부터 데이터가 완전히 갈라져서 적재되도록 라우팅 로직을 수정해 주세요.
   - **시한성/기간성 데이터 (시작일·종료일 존재):** 무조건 `events` 테이블로만 적재.
   - **상시성/고정 시설 데이터 (기간 없음, 연중 상시):** 무조건 `open_spaces` 테이블로만 적재.
   - 서로 무분별하게 크로스되거나 섞여 들어가는 기존의 모호한 쿼리/로직은 전면 차단하고 제거해 주세요.

데이터 수집 파이프라인에서 '서울시 공공서비스 예약' 등에서 수집되는 공공 대관 및 예약형 시설/프로그램 데이터가 잘못 적재되고 있는 라우팅 경로를 수정해 주세요.

### 📌 수정 요구사항

1. **'서울시 공공예약' 소스 데이터의 라우팅 변경**
   - 수집처가 '서울시 공공서비스 예약'이거나, 핵심 운영 방식이 '대관/사전 예약제'인 공공 체육시설·프로그램 데이터는 `open_spaces`(상시 스팟)가 아닌 **`events` (시한성 이벤트/예약 픽) 테이블로 강제 라우팅**되도록 수정해 주세요.
   - 현재 체육시설 대분류와 축구장, 농구장 등의 시설 대관 예약관련 seoul_public_reservation 원천 데이터 관련 open_spaces에서 보이고 있는데 evnets쪽으로 이관해주세요
2. **기존 잘못 적재된 데이터 마이그레이션/차단**
   - 이미 `open_spaces`에 섞여 들어간 공공 예약/대관 데이터들을 식별하여 `events` 쪽으로 올바르게 재배치(또는 마이그레이션)하고, 앞으로는 원천 소스 및 속성 기준으로 두 영역이 완전히 분리되어 적재되도록 정비해 주세요.

---

[개선사항 10]
시한성 이벤트(`events`)와 상설 스팟(`open_spaces`)을 파이프라인 단계에서는 분리하되, 데이터 모델과 UI 상에서 유기적으로 연결(양방향 링킹)될 수 있도록 구조를 구현해 주세요.

### 📌 핵심 요구사항

1. **데이터 모델링 (FK 링킹)**
   - `events` 테이블에 `open_spaces`를 참조할 수 있는 외래키 컬럼(`open_space_id`)을 추가해 주세요.
   - 이벤트 수집 시 장소 매칭 로직을 두어, 일치하는 상설 스팟이 있으면 자동 매칭하고, 매칭되는 스팟이 없는 경우 관리자가 수동으로 스팟을 지정하거나 신규 등록하여 연결할 수 있도록 관리자 툴 플로우를 지원해 주세요.

2. **유저 화면 양방향 연동**
   - **Event ➔ Spot:** 이벤트 상세 화면의 장소/위치 영역을 탭했을 때, 연결된 `open_spaces` 상세 페이지로 이동할 수 있도록 연동해 주세요.
   - **Spot ➔ Event:** 상설 스팟(`open_spaces`) 상세 페이지 내에, **현재 해당 장소에서 진행 중이거나 예정된 시한성 이벤트(`events`) 목록**을 실시간으로 조회하여 보여주는 섹션을 추가해 주세요. (진행 중인 이벤트가 없는 경우 해당 섹션은 숨김 처리)

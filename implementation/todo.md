## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---

[개선사항 1] 당신은 PostgreSQL/Supabase 및 데이터 정제/배치 로직 구현에 능숙한 시니어 백엔드 엔지니어입니다.

대상 파일(또는 테이블 데이터): `seoul-yeyak-usetgtinfo-distinct-values-2026-09-18.md`

데이터베이스 스키마에 `연령카테고리` 컬럼을 추가하고, 아래의 비즈니스 규칙과 프로세스에 맞춰 데이터 정제 및 자동화 배치 로직을 구현해 주세요.

### 1. 연령카테고리 분류 규칙
- **OTHER 분류:** 텍스트에 "단체", "여성" 또는 "장애인" 키워드가 포함된 경우 (예: `유아단체` -> `OTHER`)
- **TEEN 분류:** 텍스트에 "초등학교고학년" 또는 "초등학교4학년이상" 키워드가 포함된 경우 (`TEEN` 범위: 초등학교 4학년 ~ 고등학생)
- **빈값 유지:** 위 조건에 해당하지 않고 기존 연령카테고리가 비어있는 경우 빈값 유지

### 2. 데이터 전처리 및 중복 제거(DISTINCT) 규칙 (613개 리스트 기준)
1. **공백 제거:** 문자열 내의 모든 띄워쓰기(공백)를 우선 모두 제거합니다.
2. **중복 병합 (DISTINCT):** 공백 제거 후 완전히 중복되는 항목들은 하나로 병합합니다.
3. **병합 시 충돌 해결 규칙:**
   - 병합하려는 항목 중 한쪽만 연령카테고리가 있다면 **값이 있는 것**을 사용합니다.
   - **둘 다 값이 있는데 서로 다를 경우, 가장 위에 있는(먼저 등장한) 값을 우선 사용**합니다.

### 3. 향후 데이터 처리를 위한 배치 잡(Batch Job) 제약 조건
- 신규 유입되는 데이터에 대하여 연령카테고리를 자동 집어넣는 배치를 구성할 때, **원천 데이터 수집 시점에 실시간으로 수행하지 마세요.**
- 반드시 **데이터가 DB에 전부 UPSERT된 이후에, DB에 쌓인 데이터를 대상으로 사후에 작업하는 배치 잡(Batch Job)**으로 설계해 주세요.

### [출력 요구]
- 위 규칙들을 반영하여 데이터를 정제하고 일괄 처리하는 스크립트(Python 또는 Node.js)와, DB 상에서 UPSERT 이후 실행될 후행 배치 잡의 구조 및 SQL/코드 로직을 작성해 주세요.

---

[개선사항 2] 당신은 PostgreSQL/Supabase 및 대용량 데이터 처리 백엔드 배치 시스템에 정통한 시니어 엔지니어입니다.
현재 서비스의 백엔드 배치/수집 시스템에서 발생한 총 4가지 이슈에 대해 **원인 분석과 함께 즉시 적용 가능한 해결 코드 및 SQL 패치(조치 방안)**를 작성해 주세요.

### [점검 대상 이슈 4가지]

#### 1. GG_CULTURE_EVENTS (경기데이터드림) 수집 중 파싱/저장 에러 (312건)
- **현상:** 총 3,270건 수신 중 2,958건은 정상 저장되었으나, 312건은 에러로 처리됨.
- **점검 요청:** 데이터 파싱 및 검증 단계에서 어떤 필수 값 누락이나 타입 에러로 인해 312건이 거부(Exception)되고 있는지, 코드 상의 유효성 검사 로직(Validation) 또는 원본 데이터 특성을 진단해 주세요.

#### 2. SEOUL_CULTURE_EVENTS (서울시 문화행사 정보) 수집 중 파싱/저장 에러 (538건)
- **현상:** 총 19,533건 수신 중 18,995건은 정상 저장되었으나, 538건은 에러로 처리됨.
- **점검 요청:** 서울시 공공데이터(`culturalEventInfo`)를 파싱하고 DB에 적재하는 과정에서 어떤 필드 누락, 날짜/텍스트 포맷 이슈, 또는 중복/제약 조건 위반 등으로 인해 538건이 거부되고 있는지 진단해 주세요.

#### 3. MATCH_EVENTS_TO_OPEN_SPACES 타임아웃 (`canceling statement due to statement timeout`)
- **현상:** 이벤트와 스팟(Open Spaces)을 좌표/명칭 기준으로 자동 매칭하는 과정에서 쿼리 실행 제한 시간 초과로 실패.
- **점검 요청:** 매칭 쿼리에서 Full Table Scan이 발생하고 있는지, 인덱스(Index)가 누락되었거나 카테시안 곱(Cartesian Product) 같은 비효율적인 조인이 발생하고 있는 원인을 진단해 주세요.

#### 4. REFRESH_EVENTS_FILTER_OPTIONS_CACHE 타임아웃 (`canceling statement due to statement timeout`)
- **현상:** 이벤트픽 필터 옵션 Materialized View를 갱신(`REFRESH`)하는 과정에서 타임아웃 발생.
- **점검 요청:** 해당 Materialized View를 생성하는 원본 SELECT 쿼리가 너무 무겁거나, 대용량 집계(GROUP BY, DISTINCT 등) 연산에서 병목이 생기는 이유를 진단해 주세요.

---

### [출력 요구]
- 위 4가지 이슈에 대해 각 원인과 추정되는 문제 지점을 코드/쿼리 관점에서 설명해 주세요.

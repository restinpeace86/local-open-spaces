## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---
### 작업 내용:  연령 매핑 체계 (events 전용)

- [x] **[Dry-Run] target_audience 정제 파이프라인 사전 영향도 시뮬레이션 분석 (8대 세분화 체계 및 원천 필드 동적 탐색 우선 적용)** — 완료(2026-08-27, DB 미반영 순수 시뮬레이션). 결과: `implementation/2026-08-27-target-audience-8tier-dryrun.md`, 상세 리포트: `docs/target-audience-8tier-dryrun-report.md`. 최종 NULL 잔여 1,797건(50.48%). "성인" 단독 값 처리 방침 등 4건은 대표 승인 대기 미결 사항으로 보고서에 남김(임의 확정 안 함).
  - **[핵심 지침 - 절대주의]**
    1. 실제 DB 수정 및 데이터 정제 작업은 절대 수행하지 마라. (오직 Read-Only 상태로 시뮬레이션 선행 후 분석 결과 리포트 필수)
    2. 불필요하고 실패율이 높은 OCR 및 웹 스크래핑을 전면 배제하고, [0순위 원천 필드 동적 탐색 및 우선 참고 ➔ 0단계 역방향 소거 ➔ 1단계 세분화 타겟 특화 상속 ➔ 2단계 명확 텍스트 파싱]의 단계별 파이프라인으로만 자동 정제 시뮬레이션을 실행하라.
    3. 대상은 오직 `events` 테이블의 `is_active = true` 유효 데이터만 대상으로 하며, `open_spaces`는 완벽히 제외한다.
    4. 과잉 소거 방지를 위해 '시민', '주민', 단독 '부모' 단어는 역방향 소거 대상에서 완전히 제외하고, 100% 성인/전문가/학술 전용 키워드만 핀포인트 소거한다.
    5. 정제 후 최종적으로 target_audience IS NULL로 남아 수동 검수로 넘어가게 되는 최후 잔여건수를 수치화하고, 이 NULL 건수를 극단적으로 줄일 수 있는 고효율 추가 키워드를 제안하라.

  - **[분류 알고리즘 규칙 (8대 세분화 체계: INFANT, KIDS_PRE, KIDS_SCHOOL, FAMILY, TEEN, YOUTH, SENIOR, ALL)]**
    1. **[0순위: 원천 필드 동적 탐색 및 우선 참고]**
       - 백필된 raw_data 내부에서 연령대를 참고할 수 있는 원천 필드(`USE_TRGT`, `target_age_group`, `USETGTINFO` 등)를 확인하되, API 소스별로 필드명이 다를 수 있으므로 raw_data 구조를 먼저 면밀히 스캔하여 연령 관련 의미를 담은 유사 필드들을 폭넓게 탐색한다.
       - 해당 필드에 값이 존재할 경우, 텍스트 파싱에 앞서 이 원천 필드 값을 우선적으로 분석하여 8대 세분화 타겟 태그로 1:1 매핑을 시도한다.
       - 원천 필드 값이 비어있거나(`NULL`) 명확하지 않은 경우에만 아래의 텍스트 파싱 단계로 진입한다.
    2. **[0단계: 역방향 소거 최우선 적용 - Negative Override]**
       - 원천 필드가 없거나 텍스트 분석이 필요한 경우, 제목(`title`) 및 설명(`contents`/`description`) 내 성인/전문가/학술 전용 소거 키워드 포함 시 `INFANT`, `KIDS_PRE`, `KIDS_SCHOOL`, `FAMILY` 대상에서 즉시 배제:
         - 학술/행정: `학술대회`, `세미나`, `학술`, `포럼`, `심포지엄`, `간담회`, `설명회`, `공청회`, `봉사활동`, `민원`
         - 전시/전람회: `개인전`, `정기전`, `회원전`, `초대전`, `교류전`, `학위청구전`, `졸업전시`, `동문전`, `동호회전`
         - 음악/공연: `독주회`, `독창회`, `리사이틀`, `귀국 연주회`, `정기 연주회`, `동문 음악회`
         - 부모/학부모: `학부모`, `부모교육`, `부모교실`, `부모특강`, `양육`, `성교육`, `지도자`, `지도법`, `자녀교육`, `아동학대예방` (※ 단독 '부모' 단어 소거 제외)
         - 자격증/직무: `자격증`, `강사`, `전문가`, `재테크`, `부동산`, `창업`, `취업`, `역량강화`, `실무`, `직무교육`, `마케팅`
         - 연령/대상: `성인`, `어르신`, `시니어`, `실버`, `은퇴`, `청년` (※ '시민', '주민' 단어 소거 제외)
    3. **[1단계: 8대 세분화 타겟 특화 상속]**
       - 0단계 소거를 통과한 데이터 중, 중분류명/원천분류 자체로 타겟이 100% 명확한 카테고리 자동 부여 (`INFANT`, `KIDS_PRE`, `KIDS_SCHOOL`, `FAMILY`, `TEEN`, `YOUTH`, `SENIOR`, `ALL`).
    4. **[2단계: 명확 텍스트 파싱]**
       - 0·1단계를 거친 남은 데이터 중, 제목/본문에 각 세분화 타겟층 키워드가 명확히 매칭될 때만 해당 태그 부여. 매칭되지 않는 항목은 억지로 넣지 않고 **그대로 `NULL`로 유지**.
    5. **[3단계: 어드민 정밀 수동 검수 (최후 잔여건)]**
       - 0~2단계를 거치고도 판단이 불가능하여 `target_audience IS NULL`로 남는 최후 잔여건수 산출 및 고효율 추가 키워드 제안.

- [ ] **[레거시 백필 실행 및 Target Audience 10대 분류 체계 시뮬레이션]**
  - **[핵심 원칙 - 절대주의]**
    1. **레거시 백필은 실제 DB 적용**: `source` 및 `raw_data`가 비어 있는 구버전(`SEOUL_RESERVATION_*`, `source=null`) 데이터들은 실제 DB에 원천 데이터 및 소스명을 정상적으로 UPDATE 반영한다.
    2. **10대 분류 시뮬레이션은 Read-Only**: 백필 완료 후 `events.target_audience` 정제 작업은 실제 DB 변경 없이 오직 메모리 상에서 10대 체계 Dry-run 시뮬레이션만 수행하고 결과를 보고한다.
    
  - **[상세 작업 내용]**
    1. **레거시 소스 실데이터 백필 실행**: 
       - `source=null` 및 `raw_data` 공백인 대상 데이터 분석 및 실제 DB 백필 스크립트 작성·실행 (정상 `source` 및 `raw_data` 메타데이터 복구).
    2. **10대 세분화 체계 룰 세팅**:
       - 총 10개 태그 체계 정의: `INFANT`, `KIDS_PRE`, `KIDS_SCHOOL`(키즈 키워드 포함), `FAMILY`, `TEEN`(중고등 및 문맥 제한된 학생 키워드 포함), `YOUTH`, `ADULT`(성인 단독), `SENIOR`, `ALL`(전연령 행사), `FACILITY`(캠핑장, 주민센터 다목적실, 영화촬영 등 시설·장비 대관 전용).
    3. **10대 체계 기준 2차 Dry-run 시뮬레이션**:
       - 백필이 완료된 최신 `events` 데이터를 대상으로 10대 태그 매핑 알고리즘을 메모리에서만 재시뮬레이션 수행.
       - 단계별 퍼널(0순위 원천 필드, 1단계 카테고리/FACILITY 판정, 2단계 텍스트 파싱) 및 최종 NULL 잔여 건수 통계 리포트 산출 후 대표 승인 요청.
  - **검증**: `npx tsc --noEmit` 및 백필/시뮬레이션 스크립트 실행 검증.

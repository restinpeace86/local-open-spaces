## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---
# To-Do List

[개선사항1] 데이터 수집 배치 파이프라인(현재 12단계)의 아키텍처를 개선하려고 합니다. 

현재 10개가 넘는 외부 공공 API(서울시, 경기도, TourAPI 등)를 수집하는 과정에서, 특정 API 서버가 타임아웃이나 에러를 낼 경우 **전체 배치 프로세스가 연쇄적으로 실패(Cascading Failure)하거나 `exit 1`로 종료되는 강결합(Tight Coupling) 문제**가 있습니다. 또한 마지막 캐시 갱신 단계에서 `statement timeout` 에러도 발생하고 있습니다.

다음 요구사항에 맞춰 코드를 **독립 격리형(Fail-Safe) 구조**로 리팩토링해 주세요.

---

### 🛠️ 주요 요구사항

Node.js 기반의 데이터 수집 파이프라인 구조를 **완전한 독립 분리형 마이크로 배치(Decoupled Independent Batch) 아키텍처**로 전면 개편하려고 합니다.

현재 10개가 넘는 외부 공공 API(서울시, 경기도, TourAPI 등)와 내부 정제/캐시 단계들이 하나의 거대한 모놀리식 스크립트로 묶여 있어서, 특정 API 하나가 타임아웃을 내거나 실패하면 전체 배치가 휘둘리는 문제가 있습니다.

다음 요구사항에 맞춰 코드를 리팩토링해 주세요.

---

### 🛠️ 핵심 요구사항

1. **원천 소스별 수집 스크립트의 완전한 물리적/논리적 독립**
   - 각 원천 소스(API) 수집 및 Supabase Upsert 로직은 하나의 파일에서 순차 처리되는 구조가 아니라, **각각 독립적으로 실행될 수 있는 개별 스크립트(또는 독립된 함수/모듈 단위)**로 완전히 분리해 주세요.
   - A 소스가 에러로 죽거나 `exit 1`을 뱉더라도, B 소스나 후속 정제 작업의 실행에 단 1도 영향을 주지 않아야 합니다. (공유 상태 및 연쇄 의존성 원천 차단)

2. **오케스트레이터(Runner)와 워커(Worker)의 분리**
   - 만약 전체를 한 번에 트리거하는 메인 러너가 필요하다면, 각 워커(수집기)를 독립된 프로세스나 독립된 비동기 태스크로 호출하고, 개별 워커의 실패가 전체 러너의 강제 중단을 유발하지 않도록 **독립 실행 구조(`Promise.allSettled` 등 활용)**로 묶어주세요.
   - 특정 소스가 실패해도 해당 소스만 에러 로그를 남기고, 전체 파이프라인 루프는 정상적으로 다음 소스로 진행되어야 합니다.

3. **DB 캐시 갱신 쿼리 타임아웃(`statement timeout`) 분리 및 최적화**
   - 마지막 단계의 `REFRESH_SIGUNGU_OPTIONS_CACHE`에서 발생하는 `statement timeout` 문제를 해결하기 위해, 이 캐시 갱신 작업 역시 수집 배치와는 별개로 돌거나 쿼리가 무겁지 않게 최적화(또는 증분 처리)되도록 분리해 주세요.

---
현재 발생하고 있는 배치 구조의 문제점을 해결할 수 있도록, 러너(Runner) 메인 로직과 에러 핸들링 코드를 위 원칙에 맞게 리팩토링해 주세요.

[개선사항2] 현재 개발 중인 관리자 대시보드의 **[open_spaces] 도메인 백엔드 데이터 적재 및 Upsert(업서트) 로직**에 대해, 소스별 원천 분류 체계 수용 방식과 기존 검수 데이터 보호 안전장치를 구현해줘.

---

### 🧩 1. 원천 소스별 고유 분류 체계 매핑 (Source-Native Classification)
공공데이터를 수집·적재할 때, 억지로 공통된 톱다운 분류를 강제하지 않고 **각 원천 데이터가 가진 고유의 분류 체계를 표준 대/중분류 컬럼에 유연하게 매핑**하여 적재할 것.
- **소스 예시 (서울시 공공예약 등):** 
  - `MAXCLASSNM` (원천 대분류) ➡️ DB의 `standard_major_category` (표준 대분류)로 매핑
  - `MINCLASSNM` (원천 중분류) ➡️ DB의 `standard_middle_category` (표준 중분류)로 매핑
- **다른 소스 (행안부 어린이놀이시설 등):** 
  - 해당 소스의 원천 분류 필드(`instlPlaceCdNm` 등)를 체계에 맞게 대/중분류에 매핑
- **속성 태그 (Attributes):** 
  - 민간/공공 여부(`prvtPblcYnCdNm` 등)처럼 소스마다 유무가 다른 성격은 분류 체계가 아니라 **선택적 속성 태그(Attribute/Tag)** 필드로 분리하여 적재할 것.

---

### 🛡️ 2. 핵심: 기존 관리자 수동 검수 데이터 보호 (Upsert Guard) - [매우 중요]
현재 DB에는 **이미 관리자가 수동으로 검수·입력해 둔 `exposure_category` (노출 중분류) 데이터들이 이미 존재**합니다. 따라서 새로운 공공데이터 배치가 돌아서 기존 `open_spaces` 스팟과 매칭되어 갱신(Upsert)될 때 다음 규칙을 철저히 엄수해 줘:
- **노출 중분류(`exposure_category`) 절대 불변:** 이미 값이 들어가 있거나 존재하는 레코드의 경우, 공공데이터 싱크로 인해 **절대 덮어씌워지거나 초기화(NULL 처리)되지 않고 기존 값을 100% 그대로 보존(Preserve)**해야 함. (관리자의 명시적인 수동 정정이 없는 한 기존 노출 중분류 값은 절대 건드리지 말 것)
- **뱃지(`badges`) 보호:** 이미 저장된 뱃지 데이터 역시 업서트 시 덮어씌워지지 않도록 함께 보호할 것.
- **업서트 허용 범위:** 공공데이터 싱크 시 갱신되는 대상은 오직 원천 메타데이터(주소, 연락처 등), 원천 소스 기반의 표준 대/중분류, 그리고 속성 태그들로만 엄격히 한정할 것.

위 요구사항을 모두 반영하여 `open_spaces` 도메인의 백엔드 데이터 모델, 파싱/매핑 로직, 그리고 Upsert 쿼리/코드를 작성해 줘.


[개선사항3] 현재 개발 중인 [open_spaces] 도메인의 백엔드 블로그 수집/검증 파이프라인 및 관리자 워크벤치에, 데이터 오탐을 줄이고 검수 효율을 극대화하기 위한 '지능형 자가 치유(Self-Healing) 및 텍스트 정규화' 기능을 구현해 줘.

---

### 1. 스마트 검색 쿼리 조합 및 자가 치유(Self-Healing) 리트라이 로직
- **1차 스마트 검색:** 네이버 블로그 API 검색 시, 단순 상호명만 던지지 말고 DB에 저장된 주소 체계를 활용하여 `[상호명] + [지점명 또는 동/읍/면]` 조합으로 1차 검색 쿼리를 생성할 것.
- **실시간 적합성 검증(Validation):** 가져온 블로그 본문 텍스트 내에 해당 스팟의 핵심 키워드(지역명, 지점명 등)가 포함되어 있는지 실시간으로 검사할 것.
- **자가 치유(Auto-Refetch):** 미스매치(타 지역 지점 혼입 등)나 정보 부족으로 불합격한 블로그 개수(N개)를 파악하고, 상위 행정구역명을 결합한 확장형 스마트 쿼리로 자동 변환하여 **부족한 개수(N개)만큼만 API를 정밀 재호출 및 교체**하는 자동 리트라이 파이프라인을 구축할 것.
- **재시도 횟수 제한 (Max Retry Cap):** 무한 루프 및 API 쿼터 낭비를 방지하기 위해 스마트 재검색(Auto-Refetch)은 **최대 1회(또는 최대 2회)**로 엄격히 제한할 것.
- **단계적 쿼리 확장:** 
  - 1차 시도: `[상호명] + [동/읍/면]` 조합 예) 쿠우쿠우 명월점의 경우 쿠우쿠우 명월로 검색 .. .. 명월동에 있어서 명월점이지만 명월점으로 검색할 경우 안나오고 명월로 검색하면 나오는 case도 있음. 동/읍/면에 대하여 명월동 혹은 명월점이 아닌 명월로만 검색키워드 가져가는게 검색 성공율이 높음
  - 2차 시도(최종): 실패 시, 더 넓은 상위 행정구역인 `[상호명] + [시/군/구]` 조합으로 확장하여 재시도 ex) 쿠우쿠우 중구 라던가... 
- **최종 Fallback 처리:** 최대 재시도 횟수를 초과했음에도 정상 블로그를 확보하지 못한 경우, API 호출을 중단하고 해당 슬롯에 `[⚠️ 자동 매칭 실패 - 수동 확인 필요]` 경고 배지를 고정 노출하여 관리자 검수 큐로 안전하게 이관할 것.

### 2. 공백 무시 및 유의어 확장 텍스트 정규화 엔진
- **공백 무시 매칭:** 뱃지 키워드(예: '주차', '아기의자' 등) 매칭 및 하이라이팅 시, 모든 공백을 무시(Whitespace-Insensitive)하여 처리할 것 (예: "아기의자", "아기 의자", "아 기 의 자" 모두 동일 매칭).
- **유의어 확장 사전 적용:** 대표 키워드에 딸린 변형 표현들(예: `아기의자` ↔ `유아용 의자`, `유아 의자` / `주차` ↔ `주차장`, `차댈곳`)을 유의어 세트로 묶어 함께 인식되도록 할 것.

### 3. 지역/지점명 및 뱃지 키워드 동시 하이라이팅 & 워닝 UI
- **동시 하이라이팅:** 관리자 워크벤치 UI 및 텍스트 파서에서 타겟 뱃지 키워드뿐만 아니라 **해당 스팟의 고유 지역/지점명 키워드(예: '명월', '분당' 등)도 블로그 본문에서 찾아내어 노란색 배경(<mark> 등)으로 함께 하이라이팅** 처리할 것.
- **워닝 배지 표시:** 자가 치유 과정에서 걸러지지 못했거나 지역명이 불일치하는 블로그 슬롯에는 워닝 배지(Warning Badge)를 노출하여 관리자가 직관적으로 식별할 수 있게 할 것.

위 요구사항을 반영하여 백엔드 파서/검증 서비스 로직과 관련 API, 그리고 관리자 UI 렌더링 스펙을 작성해 줘.

---

[개선사항4] 현재 개발 중인 [open_spaces] 데이터 파이프라인과 관리자 워크벤치(UI 및 백엔드)를, 향후 다양한 공간 카테고리(식당, 키즈카페 등)로 유연하게 확장할 수 있도록 '뱃지 및 카테고리 룰 구조'를 다음과 같이 설계·구현해 줘.

### 1. 카테고리별 뱃지 및 룰의 완전 독립 설계
- 공통 뱃지 같은 복잡한 상위 개념을 두지 말고, **각 카테고리(식당, 키즈카페 등)가 자신만의 뱃지 목록, 노출 중분류, 키워드 맵핑을 완전히 독립적으로** 들고 있도록 설계할 것. (예: '주차 완비' 같은 항목이 여러 카테고리에 공통으로 필요하더라도 각 카테고리 Config 내에 각각 정의할 것)

### 2. 카테고리별 설정 파일(Config / JSON) 구조 정의
- 백엔드 코드나 프론트엔드 UI 내부에 특정 카테고리의 뱃지나 노출 중분류를 하드코딩하지 말고, 아래와 같은 **카테고리별 독립 룰셋(Config)** 구조로 분리할 것.
  예시..
  ```json
[
  {
    "category_id": "restaurant",
    "exposure_categories": ["키즈친화 식당", "일반 식당"],
    "badge_groups": [
      {
        "section_title": "이동/편의",
        "badges": [
          { "id": "parking", "name": "주차 완비", "keywords": ["주차", "주차장", "차댈곳", "발렛"] },
          { "id": "stroller", "name": "유모차 가능", "keywords": ["유모차", "유모차 반입", "유모차 동반"] },
          { "id": "nursing_room", "name": "수유실 있음", "keywords": ["수유실", "모유수유"] },
          { "id": "diaper_table", "name": "기저귀 갈이대", "keywords": ["기저귀갈이대", "기저귀 교환", "기저귀대"] }
        ]
      },
      {
        "section_title": "식사/아기",
        "badges": [
          { "id": "baby_chair", "name": "아기의자", "keywords": ["아기의자", "유아용 의자", "아기 의자"] },
          { "id": "baby_tableware", "name": "유아 식기", "keywords": ["유아 식기", "아기 식기", "유아용 식기", "아이 식판"] },
          { "id": "kids_menu", "name": "키즈 메뉴", "keywords": ["키즈 메뉴", "어린이 메뉴", "아이 메뉴"] },
          { "id": "floor_seat", "name": "좌식/온돌 있음", "keywords": ["좌식", "온돌", "마루", "좌식 테이블"] },
          { "id": "private_room", "name": "룸/개별 공간 있음", "keywords": ["룸", "개별룸", "단독룸", "프라이빗 룸", "개별 공간"] }
        ]
      },
      {
        "section_title": "공간/놀이",
        "badges": [
          { "id": "kids_zone", "name": "키즈존/놀이방", "keywords": ["놀이방", "키즈존", "놀이공간", "오락실"] },
          { "id": "outdoor", "name": "야외 마당/테라스", "keywords": ["마당", "테라스", "야외", "루프탑", "정원"] }
        ]
      },
      {
        "section_title": "운영",
        "badges": [
          { "id": "reservation_required", "name": "예약 필수", "keywords": ["예약 필수", "사전 예약 필수"] },
          { "id": "reservation_available", "name": "예약 가능", "keywords": ["예약 가능", "네이버 예약", "전화 예약"] }
        ]
      }
    ]
  },
  {
     "category_id": "kids_cafe",
     "exposure_categories": ["대형 키즈카페", "프리미엄 키즈카페", "실내 놀이터"],
     "badge_groups": [
       {
         "section_title": "이동/편의",
         "badges": [
           { "id": "parking", "name": "주차 완비", "keywords": ["주차", "주차장", "차댈곳", "발렛"] },
           { "id": "stroller_parking", "name": "유모차 보관/가능", "keywords": ["유모차", "유모차 보관", "유모차 파킹"] },
           { "id": "nursing_room", "name": "수유실 있음", "keywords": ["수유실", "모유수유"] },
           { "id": "diaper_table", "name": "기저귀 갈이대", "keywords": ["기저귀갈이대", "기저귀 교환", "기저귀대"] }
         ]
       },
       {
         "section_title": "놀이/시설",
         "badges": [
           { "id": "trampoline", "name": "트램폴린/방방", "keywords": ["트램폴린", "방방", "방방이", "점핑존", "점프"] },
           { "id": "ball_pool_jungle", "name": "볼풀장/정글짐", "keywords": ["볼풀", "볼풀장", "정글짐", "클라이밍", "미끄럼틀"] },
           { "id": "hinoki_sandbox", "name": "편백존/모래놀이", "keywords": ["편백", "편백존", "편백나무", "모래놀이", "모래존"] },
           { "id": "baby_zone", "name": "베이비존(영유아 전용)", "keywords": ["베이비존", "영유아존", "아기들 노는 곳", "돌쟁이"] }
         ]
       },
       {
         "section_title": "부대시설/보호자",
         "badges": [
           { "id": "parent_relax", "name": "부모 쉼터/안마의자", "keywords": ["안마의자", "릴렉스존", "부모 쉼터", "안마기", "안마"] },
           { "id": "party_room", "name": "파티룸/개별 룸", "keywords": ["파티룸", "대관", "생일파티", "단독룸", "프라이빗 룸"] },
           { "id": "kids_cafe_food", "name": "식사 및 간식 판매", "keywords": ["식사", "떡볶이", "주먹밥", "식음료", "음식 맛집", "매점"] }
         ]
       },
       {
         "section_title": "운영",
         "badges": [
           { "id": "reservation_required", "name": "예약 필수", "keywords": ["예약 필수", "사전 예약 필수", "회차별 예약"] },
           { "id": "reservation_available", "name": "예약 가능", "keywords": ["예약 가능", "네이버 예약", "전화 예약"] }
         ]
       }
     ]
   }
]


. 프론트엔드: 콤보박스 연동 실시간 동적 하이라이팅 및 뱃지 스위칭 UI
최초 모달 오픈 시: 공공데이터 원본 기반의 디폴트 카테고리(예: 식당) 기준 뱃지 체크박스와, 해당 keywords_mapping에 맞춰 노란색 배경(<mark> 등)으로 하이라이트된 블로그 텍스트를 먼저 렌더링할 것.

콤보박스 변경 시 (클라이언트 사이드 동적 처리):

관리자가 모달 내의 '노출 중분류' 콤보박스에서 다른 카테고리를 선택하는 순간, 서버에 API를 재요청(Refetching)하지 않고, 브라우저에 이미 로드된 블로그 원문 텍스트에 대해 새로 선택된 카테고리의 keywords_mapping 규칙으로 노란색 하이라이트를 즉시 지우고 다시 렌더링할 것.

동시에, 하단의 [뱃지 체크박스 목록]도 선택한 카테고리의 전용 뱃지들로 실시간 교체될 것.


---
[참고사항]
[노출될 중분류]

키즈/놀이시설 (대분류)
- 키즈카페 / 실내놀이터 (중분류)
- 키즈친화 식당 (놀이시설 포함) (중분류)
- 물놀이장 / 바닥분수 (시즌성) (중분류)
- 실내 체험·놀이 공간 (중분류)

농장/체험 (대분류)
- 체험농장·농원 (중분류)
- 휴양마을 (중분류)

자연/공원 (대분류)
- 대형 근린공원 / 잔디광장 (중분류)
- 생태공원 / 산책로 (중분류)
- 수목원 / 식물원 (중분류)
- 캠핑장 / 피크닉장 (중분류)

문화시설 (대분류)
- 어린이 도서관 (중분류)
- 어린이 과학관 / 박물관 (중분류)
- 미술관 / 전시체험관 (중분류)




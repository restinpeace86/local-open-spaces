## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---

### Task 목록
- [ ] **[Task 9-6-19] 위치 정보 미비 데이터의 5대 카테고리 강제 흡수 및 UI 예외 섹션 완전 배제** 🧹📍 — **[스킵 (보류)]**
  - **세부 작업 지시**:
    1. **'기타' 영역 UI 노출 완전 차단**:
       - 메인 화면 및 이벤트픽 피드 내에 '경기도 기타', '기타 지역' 등 파편화된 별도 카드 섹션이나 뱃지가 표출되는 UI 부가 로직을 완전 제거할 것.
    2. **위치 무관 5대 카테고리 100% 흡수 매핑**:
       - 상세 주소나 위경도 좌표가 없는 공공 데이터라 할지라도 행사 성격(제목/설명/타겟)을 기반으로 반드시 5대 카테고리(`체험·클래스`, `야외·자연`, `전시·박물관`, `공연·축제`, `키즈·액티비티`) 중 하나 이상에 분류·적재되도록 처리할 것.
    3. **지역 필터 피딩 우회(Fallback) 보장**:
       - 유저 지역이 '경기도'일 때, 세부 시·군·구 좌표가 누락된 경기도 수집 건도 해당 카테고리 피드에 정상 포함되어 피딩되도록 DB 쿼리 및 피드 필터링 로직 보완할 것.
  - **① 상세 스킵 사유**:
    - `project/decision-log.md` **Decision 009**가 사용자 채팅 승인으로 명시적으로 확정한 구조: `location_precision = 'UNKNOWN'`(시/군명조차 매칭 안 된 위치 미상 행사)은 지도/주변 반경 검색에는 노출하지 않되, 메인 페이지의 별도 **"경기도권 기타" 섹션**(`getProvinceWideEvents()`, `src/lib/home/get-home-feed.ts:454-475`)에서만 노출하도록 이미 구현·운영 중이다. Task 9-6-19의 "① '기타' 영역 UI 완전 차단"은 이 결정을 정면으로 번복하는 지시다.
    - `spec/data/ai-rule.md` 3.3(5대 UI 카테고리 매핑)은 원본 `category`/`event_type` 값을 기준으로 한 매핑 레이어로만 정의되어 있으며, 4.1 예외 처리 원칙은 "AI가 카테고리를 명확히 판별하기 어려운 경우 임의로 생성하지 않고 기본값(`ETC`/`GENERAL`)으로 분류"하도록 규정한다. Task 9-6-19의 "② 제목/설명 텍스트 기반 100% 강제 흡수 매핑"은 Spec에 정의되지 않은 신규 텍스트 기반 분류 규칙이며, 모호한 경우도 강제로 5대 카테고리에 끼워 맞추라는 지시라 4.1 예외 처리 원칙과 상충한다.
    - 두 지시 모두 CLAUDE.md 제3장 제4조(추측 금지), 제7장 제3조(임의 비즈니스 로직 생성 금지)·제6조(역할 침범 및 기획 변경 금지)에 해당하여 구현 AI가 임의로 착수할 수 없다.
  - **② 재개를 위한 선행 작업**:
    1. 기획 AI가 Decision 009를 대체/수정하는 신규 Decision(예: Decision 013)을 `project/decision-log.md`에 기록하여 "경기도권 기타" 섹션 폐지 및 UNKNOWN 위치 데이터의 5대 카테고리 흡수 방침을 공식 승인해야 한다.
    2. `spec/data/ai-rule.md` 3.3/4.1에 제목/설명 텍스트 기반 카테고리 강제 분류 규칙(구체적 키워드 매칭 로직·모호 시 처리 기준 포함)을 신설·개정해야 한다.
    3. 위 Decision 및 Spec 개정이 완료된 이후에만 본 Task를 "구현 가능"으로 재개할 수 있다.

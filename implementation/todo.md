- [ ] **[Task 9-6-10] 하단 5대 탭 '나드리픽' 브랜드 구조 개편 & 지도/피드 수리** 🧭 — **스킵 (보류, 2026-08-25 재확인)**
  - **작업 목표**: 하단 탭을 '스팟픽 - 장소픽 - 이벤트픽 - 찜 - 마이' 5대 탭 구조로 재정립하고, '장소픽' 지도의 상시 공간 전용 단일화 및 계층 클러스터링 완료.

  - **세부 작업 지시**:
    1. **하단 네비게이션 탭 라인업 재배치 (`bottom-nav.tsx`)**:
       - **1번 탭 (추천픽)**: (카테고리+가격+거리 3조건 필터 ➔ 1차 DB / 2차 AI TOP 3 추천 레이아웃) ex) 가격이 5000원 ~10000원 사이로 유저가 세팅할경우 1차 DB에서 유료인 것들 필터링 2차에서 가격에 대한 정보 AI로 탐색
       - **2번 탭 (스팟픽)**: 상시 공간 전용 지도 탐색 뷰 (`/nearby`) (open-spaces)
       - **3번 탭 (이벤트픽)**: 오늘/이번 주말 당일 한정 행사 카탈로그 (`/events/today`) (events)
       - **4번 탭 (찜)**: 저장한 관심 목록
       - **5번 탭 (마이)**: 마이페이지/설정
    2. **'장소픽' 지도 상시 공간 단일화 & 계층 클러스터링**:
       - 파란 원/반경 칩 제거, 상시 공간 5대 카테고리 필터 유지.
       - 줌 레벨별 계층 클러스터링 적용 및 전역 위치 변경 시 실시간 panTo 연동.
    3. **'스팟픽' & '이벤트픽' 피딩 쿼리 정립**:
       - '이벤트픽': 오늘 마감/당일 한정 이벤트 엄격 피딩 (서울/경기 수도권 통합).

  - **스킵 사유 (자율 실행 세션 사전 확인 결과)**:
    - **Decision 008 자체 조건 미충족**: Decision 008 "영향" 항목은 하단 탭 구조 변경(항목 2)을 "코드 마이그레이션 대기 (미착수, **별도 승인 필요**)"로 명시했다. 이 작업을 실행하려면 먼저 기획 AI가 새 Decision(예: Decision 010)을 `project/decision-log.md`에 기록해야 하는데, 아직 없다.
    - **spec/map/spatial-search.md 2.2와 정면 충돌**: 승인된 Spec은 `/nearby` 지도가 "시한성 정보(events) 기본 활성화 + 상시 시설(open_spaces)은 토글 버튼으로만 활성화"하는 **레이어 토글 정책**을 명시한다. 그런데 워킹 트리에 이미 커밋되지 않은 이전 세션의 미완성 변경분(`src/components/map/map-explorer.tsx`)이 `/nearby`를 `getNearbySpacesAndEvents(..., 'SPACE')`로 **상시 공간 전용 단일화**해 이벤트 레이어 자체를 지도에서 제거해버렸다 — 토글 버튼 없이 이벤트를 아예 조회하지 않는 것은 Spec 위반이다.
    - **spec/common/search.md 2.2와 충돌**: 본 작업 지시(2)는 "반경 칩 제거"를 요구하지만, 반경 선택 칩(1/5/10/20/30km)은 이 Spec 문서에 명시된 필수 UI 요소다. Spec 문서 수정 없이 제거할 수 없다(CLAUDE.md 제7장 제2조: 임의 UI 변경 금지).
    - **참고 — 워킹 트리에 미커밋 상태로 남아있는 이전 세션의 부분 작업 발견**: `src/components/nav/bottom-tabs.tsx`, `bottom-tabs.test.tsx`, `src/lib/feature-flags.ts`, `src/components/home/home-view.tsx`에 탭 라벨 변경(추천픽/스팟픽/이벤트픽, `ENABLE_RECOMMEND_TAB` 플래그로 AI 추천 화면은 미구현 상태로 안전하게 비활성화)이 이미 시도돼 있었다. 코드 주석은 "사용자 지시에 따라" 라벨만 우선 정립했다고 기록했으나, 이 역시 위 spatial-search.md 충돌(및 Decision 008의 "별도 승인" 요건)을 해결하지 못한 채 중단된 상태이며 `home-view.tsx`에는 정의되지 않은 `homeCategory` 참조 버그도 남아있다. **이번 세션에서는 이 미완성 변경분을 커밋하지 않았다** — Spec 충돌이 해소되고 Decision이 기록되기 전까지는 보존만 하고 손대지 않는다.
    - 위 상충 사항에 따라 CLAUDE.md 자율 하네스 규칙 0단계(사전 준수 확인)에 근거해 본 작업은 구현하지 않고 스킵한다. 재개하려면: (1) 기획 AI가 신규 Decision을 기록하고, (2) `spec/map/spatial-search.md`·`spec/common/search.md`를 새 방향에 맞게 먼저 갱신해야 한다.

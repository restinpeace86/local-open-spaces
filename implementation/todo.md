## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---
### 작업 내용: 7대 대분류 - 중분류 매핑 체계 (events 전용)

1. **대표 확정 7대 대분류(category_maj) 및 중분류(category_min) 매핑**:
   - **자연 / 캠핑**: 캠핑장, 산림여가, 공원탐방
   - **공공 키즈카페**: 공공키즈카페(서울형키즈카페/경기아이사랑놀이터 등), 어린이실내놀이터
   - **체험 / 농장**: 농장체험, 도시농업(가족텃밭/주말농부), 자연/과학
   - **축제 / 이벤트**: 지역축제/페스티벌, 문화행사, 광장
   - **문화 / 전시**: 공연장, 전시실, 전시/관람, 미술제작, 공예/취미, 역사
   - **배움 / 클래스**: 교육체험, 교양/어학, 교육시설
   - **스포츠 대여**: 테니스장, 풋살장, 축구장, 체육관, 골프장, 농구장, 족구장, 야구장, 다목적경기장, 배드민턴장, 탁구장, 배구장, 수영장, 운동장, 피클볼장, 스포츠

2. **소스 확장 규칙**:
   - 서울시(`SEOUL_YEYAK`) 이벤트성 중분류 36종을 포함하여, 경기도(`gg_public`), TourAPI 등 전 소스의 events 데이터에 대해 위 7대 대분류 - 중분류 체계를 확장 적용한다.
   - 단체봉사, 청년정보, 녹화장소, 민원 등 불필요 행정 중분류는 노출 대상에서 배제한다.
  
- 서울시 문화행사 및 타 API 데이터 중 과거 5대 카테고리 태그가 적용되어 있는 레거시 데이터는 이번 신규 37종 중분류 및 7대 대분류 체계로 전면 재매핑(Remap)하라.
- 과거 카테고리 명칭만으로 36종 중분류 매핑이 애매한 항목은 무리하게 추정하지 않고 category_min = NULL 로 정형화하여 어드민 수동 검수로 넘기도록 명시하라.

- [x] **[DB UPDATE] 본문 반영된 유효 events 대상 7대 대분류/중분류 실제 DB 업데이트 실행**
  - **대상 제한**: `is_active = true`인 유효 이벤트 데이터만 대상 (만료된 `is_active = false` 제외).
  - **작업 내용**: 
    1. 7대 대분류(`category_maj`) 및 중분류(`category_min`) 매핑 결과를 실제 DB에 반영 (`UPDATE`).
    2. 기존에 수동 검수로 지정된 `MANUAL` 데이터 및 커스텀 입력값은 Safe Merge 원칙을 적용하여 절대 덮어씌우지 않고 보호할 것.
  - **검증 및 보고**: 
    1. 최종 업데이트된 성공 건수, 대분류별 적재 분포 수치 리포트.
    2. `npx tsc --noEmit` 및 빌드(`npm run build`) 검증 완료 후 커밋 및 푸시.
  - **완료(2026-08-27)**: 상세 결과는 `implementation/2026-08-27-category-maj-description-real-application.md` 참고.
    매칭 성공 1,992건(55.96%) → **2,101건(59.02%)** 실반영(+109건/+3.06%p), MANUAL 보존 0건(영향 없음).
    Dry-run(Step 29) 예측치와 완전히 일치. 미해결 회귀 위험(description 반영 후 값이 바뀐 17건)은
    그대로 남아있으며, exclude 규칙 보강은 범위 밖으로 별도 승인 후 후속 작업 제안.

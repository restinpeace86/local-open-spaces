- [ ] [기능 구현] 최신 spec/ 반영(git pull) 후 ai-rule.md 기반 AI 정제 파이프라인 및 Quick 필터/뱃지 구현
  - 최신 원격 스펙(`spec/common/search.md`, `space-card.md`, `event-card.md`, `feature-flags.md`) 수신을 위해 `git pull` 우선 수행
  - `ai-rule.md` 기술 규격 및 DB 스키마 기반 AI 정제 파이프라인(`scripts/ingest/ai-tagger.mjs`) 작성 및 태깅 연동
  - UI 공간/행사 카드 정면에 AI 추출 뱃지(무료, 주차, 키즈, 실내/야외 등) 최우선 노출
  - 지도/리스트 상단 Quick Filter 칩('👶 키즈', '🎁 무료', '⚡ 오늘/주말') 스크리닝 기능 연동
  - Playwright 실브라우저로 '키즈' 및 '무료' 필터 선택 시 조건에 맞는 장소만 정상 스크리닝되는지 검증
  - **⏭️ 구현 스킵 (2026-08-20, awaiting-schema-and-ai-rule-approval):** `spec/common/search.md`, `spec/space/space-card.md`, `spec/event/event-card.md`, `spec/common/feature-flags.md`는 최근 개정되어 Quick Filter(👶 키즈, 🎁 무료, ⚡ 오늘/주말)와 카드 뱃지(주차가능 `has_parking`, 키즈친화 `is_kids_friendly`, 유모차가능 `stroller_accessible`, 실내/야외 `facility_type`, 연령대 `target_age_group`, 예약상태 `booking_status`)를 구체적 필드명으로 정의하고 있으나, 문서 우선순위(CLAUDE.md 제5장 제1조)상 상위 문서인 `project/database_schema.md`(우선순위 2)와 `spec/data/ai-rule.md`(우선순위 3, 이번 태스크가 지칭하는 "DB 스키마 기반" 원문서)는 여전히 `b282b8a`(최초 커밋) 상태 그대로이며 위 필드들을 전혀 정의하지 않는다. `public.open_spaces`/`public.events` 테이블과 `get_nearby_spaces_and_events` RPC에 해당 컬럼이 존재하지 않고(`src/types/database.types.ts` 확인), `spec/data/ai-rule.md`에도 이 필드들의 AI 추출 규칙(판별 기준·소스 텍스트·기본값 처리)이 정의되어 있지 않으며, `project/data_sources.md`의 7대 공공 API "주요 수집 항목"에도 주차/유모차/키즈친화/연령대 관련 원본 데이터가 명시되어 있지 않아 AI가 이를 추출한다면 근거 없는 값을 생성(Decision 005가 금지하는 "가짜 정보 생성")하게 될 위험이 있다. 이는 `f88f4be`, `0e5bd27` 커밋에서 이미 동일 사유로 스킵된 사안과 본질적으로 동일한 미해결 조건이다. CLAUDE.md 제3장 제4조(추측 금지), 제5장 제1조(문서 우선순위 - 상위 문서 우선)·제3조(DB 구조 변경 임의 결정 금지), 제7장 제3조(임의 비즈니스 로직 생성 금지)와 상충되어 AI 태깅 파이프라인·뱃지·키즈 Quick Filter 구현을 임의로 진행하지 않음. 기획 AI가 `project/database_schema.md`에 신규 컬럼을 확정 반영하고 `spec/data/ai-rule.md`에 해당 필드들의 AI 추출/판별 규칙(소스 데이터 근거 포함)을 명시한 뒤 재시도 필요. (참고: 🎁 무료 뱃지/필터는 기존 `is_free` 컬럼으로 이미 구현 가능하나, 본 항목이 하나의 체크리스트 단위이고 Playwright 검증 대상에 '키즈' 필터가 포함되어 있어 부분 구현 대신 전체 스킵함.)

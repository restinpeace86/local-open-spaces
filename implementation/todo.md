## 🚨 자율 실행 및 작업 진행 지침 (Strict Execution Rules)

1. **GitHub `todo.md` 기반 작업 수행**: 본 문서에 명시된 Task 목록과 세부 작업 지시를 최우선 가이드라인으로 삼아 순차적으로 작업을 진행한다.
2. **충돌 발생 시 즉시 스킵 (Skip on Conflict)**:
   - 기존 Spec 문서 (`spec/`), Decision Log (`project/decision-log.md`), 또는 기존 모듈과 구조적/논리적 충돌이 발생하는 경우, 절대로 무리하게 코드를 수정하지 말고 즉시 **[스킵 (보류)]** 처리한다.
3. **스킵 처리 시 필수 기록 사항**:
   - 충돌로 인해 작업을 스킵할 경우, 해당 Task 하단에 **① 상세 스킵 사유**를 명확히 기록한다.
   - 해당 Task를 재개하기 위해 **② 선행되어야 할 작업**(예: 신규 Decision 기록 필요, Spec 문서 선행 수정 필요 등)을 구체적인 가이드로 명시한다.
4. **원격 문서 갱신 반영 및 동기화**:
   - 원격 저장소의 `project/decision-log.md` (Decision 010) 및 `spec/map/spatial-search.md` (2.2 레이어 분리) 변경 내역을 확인하고, 충돌이 해소된 상태에서 안전하게 다음 Task를 진행한다.
5. **결과 업데이트 및 정합성 유지**:
   - 작업 완료 시 관련 테스트/빌드를 검증하고 `todo.md` 내 체크박스(`[x]`) 및 진행 상태를 최신화한다.

---

[개선사항 1] [Performance Optimization & Data Pipeline Task] 이벤트 픽 메인 화면 성능 저하 원인 분석 및 썸네일 최적화

현재 우리 서비스의 **메인 '이벤트 픽' 화면**에서 이벤트 목록을 불러올 때 로딩이 다소 느려지는 현상이 있습니다. 이미지 용량 문제뿐만 아니라 전반적인 성능 저하 원인을 진단하고, 원천데이터(공공데이터 등)의 썸네일 처리 로직을 포함하여 다음과 같이 개선 작업을 진행하고자 합니다.

## 1. 개선 목표 및 세부 요청 사항

### ① 원천데이터(Source Data) 썸네일 활용 및 규격화
- **기존 썸네일 활용:** 공공데이터나 스크래핑 등 원천데이터 자체에 이미 썸네일 이미지 URL이 포함되어 있는 경우는, 새로 생성하지 않고 **해당 원천 썸네일 이미지**를 우선적으로 사용하도록 데이터 파이프라인/업로드 로직을 수정해 주세요. 
- **원천 썸네일 사이즈 검증 및 리사이징:** 단, 원천데이터에서 제공하는 썸네일의 크기나 해상도가 우리 서비스가 정한 규격(예: 가로 300~400px, WebP 최적화)보다 클 경우, 업로드/동기화 시점에 **우리 규격에 맞게 자동으로 리사이징 및 압축**하여 저장하도록 처리해 주세요.
- 만약 원천데이터에 썸네일이 아예 없는 경우에만 기존 메인 이미지(또는 관리자 업로드 원본)를 기반으로 썸네일을 파생 생성하도록 예외 처리를 넣어주세요.
- 저희 이미지 관리는 일반 이미지 1400px로, 썸네일 이미지 300~400px로 규격화합니다.
- 들어온 이미지들에 대하여서는 상기 규격화된 이미지 사이징으로 관리될 수 있도록 하며, 이벤트픽 진입시 뜨는 프리뷰카드들에 대하여서는 썸네일 이미지에 대하여 가져오는 것으로 합니다.
  
### ② DB 및 썸네일 구조 정리
- 이벤트 테이블에 `thumbnail_url` 컬럼을 명확히 두고, 메인 목록 쿼리에서는 무거운 원본 대신 이 `thumbnail_url`을 조회하도록 구조를 정비해 주세요.

### ③ 프론트엔드 및 쿼리 레벨의 '그 외 성능 저하 원인' 진단 및 수정
- 이미지 최적화 외에도 메인 이벤트 픽 화면에서 발생할 수 있는 **다른 성능 저하 원인들**을 함께 점검하고 수정해 주세요. 
  - (예: Supabase 쿼리에서 불필요한 전체 컬럼 조회 `SELECT *` 지양, 페이징/무한스크롤 누락 여부, 날짜 필터링(`end_date`) 인덱스 부재, 클라이언트 렌더링 부하 등)
- 메인 화면 렌더링 시 Next.js의 `<Image>` 컴포넌트(`next/image`)를 활용해 `loading="lazy"` 및 적절한 최적화 옵션이 적용되도록 코드를 보완해 주세요.

현재 우리 프로젝트 구조를 바탕으로, **[1) 원천데이터 썸네일 체크/규격화 로직]**, **[2) 그 외 성능 저하 진단 및 수정 사항]**, **[3) 쿼리 및 UI 렌더링 최적화 코드]**를 포함하여 수정해야 할 파일들과 구체적인 코드를 제안해 주세요.
---

[개선사항 2] [Feature Implementation Task] 이벤트 상세/카드 화면의 링크 버튼 우선순위(Fallback) 로직 구현

현재 우리 서비스의 **이벤트 카드 및 상세 화면**에서 유저에게 제공하는 외부 링크 버튼(예약, 상세 정보 등)의 노출 로직을 체계적으로 구현하고자 합니다. 

원천데이터(공공데이터 등)의 특성에 맞춰 아래의 **3단계 우선순위(Hierarchy) 로직**을 적용하여 데이터 스키마와 프론트엔드 렌더링 코드를 작성해 주세요.

## 📋 이벤트 링크 버튼 노출 3단계 우선순위

1. **1순위: 직접 예약/신청 링크가 있는 경우**
   - **조건:** 원천데이터에 개별 프로그램의 공식 접수/예약 URL이 포함되어 있는 경우. (ex. seoul_public_reservation 등)
   - **버튼 UI:** `[예약 / 신청하기]`
   - **동작:** 해당 원천 예약 URL로 다이렉트 이동.

2. **2순위: 예약 링크는 없지만, 공식 상세/홈페이지 URL이 있는 경우**
   - **조건:** 별도의 예약 시스템은 아니지만, 해당 행사의 공식 안내 페이지나 상세 소개 URL(`DETAIL_URL` 등)이 원천데이터에 포함된 경우. (각 원천데이터를 확인하여 상세 URL 이 있는지 확인할것.. 이건 Image URL과 다름)
   - **버튼 UI:** `[상세 정보 보기]` (또는 `[공식 안내 페이지]`)
   - **동작:** 원천데이터에 포함된 공식 상세 페이지 URL로 이동하여 행사의 깊이 있는 정보(일정, 준비물 등) 제공.

3. **3순위: 둘 다 없는 경우 (Fallback)**
   - **조건:** 예약 링크와 상세 페이지 링크가 모두 누락된 경우.
   - **버튼 UI:** `[네이버에서 정보 찾아보기]`
   - **동작:** 네이버 스마트 검색 딥링크로 연결.
     - **검색어 조합 규칙:** `https://search.naver.com/search.naver?query={이벤트명}` 형태로 URL 인코딩하여 유저가 네이버에서 바로 관련 정보를 탐색할 수 있도록 방어. (이벤트명이[서초문화재단] 서리풀청년아트갤러리 전시 [Fictionally Alibi] 등 되어있을 경우 사족이 너무 많아 title로 검색하면 나오지 않음.. 서리풀청년아트갤러리 전시 서초와 같이 title 축약 + 장소로 하여 검색어 조합할 것.
     [도림생활문화센터(4분기)]모임방 (1시간 5,000원/추가 시간당 2,500원)의 경우 모임방 영등포 이렇게 하면 또 검색하면 나오지 않음.. 이경우는 도림생활문화센터 모임방 영등포 이런식으로 되어야됨.
     상기와 같이 규칙에 대하여 네이버 스마트 검색 결과로써 정보에 대한 정확도가 높을 것 같은 검색어로 조합하여 url 던져야됨

## 🛠️ 요청 사항
1. **DB 스키마 반영:** 이벤트 테이블에서 위 링크들을 처리하기 위해 필요한 컬럼 구조(예: `reservation_url`, `detail_url` 등)를 제안해 주세요.
2. **UI 렌더링 코드:** 프론트엔드(Next.js / React)에서 위 3가지 우선순위 조건에 따라 알맞은 버튼과 링크를 렌더링하는 컴포넌트(또는 함수) 코드를 작성해 주세요.

---

[개선사항 3] [Architecture & Admin Task] 데이터 파이프라인 로그 DB화 및 관리자 모니터링/수동 재수집 탭 구현

기존에 마크다운 파일(`pipeline-log.md`)에 기록하던 파이프라인 실행 로그 방식을 폐기하고, **파이프라인 실행 결과를 데이터베이스(Supabase)에 구조화된 데이터로 직접 적재**한 뒤, 관리자 화면에서 이를 시각화하고 개별 소스를 수동 재수집할 수 있는 전용 탭을 구현하고자 합니다.

다음 요구사항에 맞춰 DB 스키마, 파이프라인 로깅 로직 변경, 관리자 UI 및 수동 재수집 연동 코드를 작성해 주세요.

## 📋 주요 구현 요구 사항

### 1. DB 로그 테이블 설계 (`pipeline_logs` 또는 유사 테이블)
- 파이프라인 에이전트들의 실행 결과를 기록할 테이블 스키마를 설계해 주세요.
- **필수 포함 컬럼 예시:**
  - `id` (PK)
  - `agent_name` (에이전트/소스 식별자 - 스크립트와 100% 싱크 필수)
  - `status` (예: `SUCCESS`, `FAILED`, `RUNNING`)
  - `executed_at` (실행 일시)
  - `error_message` (실패 시 에러 내용/로그 요약)
  - `meta_data` (수집된 건수 등 추가 정보, 선택사항)
  - `description` (해당 수집기가 어떤 원천 데이터를 가져오는지에 대한 설명)
  - `period` (수집주기 : `daily`, `monthly` )
  - `updated_at` (변경 일시)

- pipeline-log.md에는 하기와 같은 컬럼들로 구성되어있음도 참조할 것.
  API 출처, 식별자 (source)	RAW 수신 건수,	events 적재 건수,	open_spaces 적재 건수,	Safe Merge 건수, 에러 건수,	비고
  
### 2. 파이프라인 로깅 로직 전환
- 각 데이터 수집 에이전트/스크립트가 실행될 때, 더 이상 `pipeline-log.md`를 수정하지 않고 **위에서 설계한 DB 테이블에 직접 실행 결과(성공/실패/에러메시지)를 INSERT/UPDATE** 하도록 로직을 수정해 주세요.


### 3. 관리자 화면 전용 탭 신설 (모니터링 & 수동 재수집)
- 관리자 대시보드 내에 **[파이프라인 관리]** 전용 탭을 만들어 주세요.
- **주요 기능:**
  - **현황판:** 최근 실행된 에이전트별 파이프라인 상태를 일자별/목록형으로 한눈에 파악할 수 있도록 렌더링 (실패한 항목은 시각적으로 강조).
  - **개별 소스 수동 재수집 연동:** 상단에 산재해 있던 수동 재수집 기능을 이 탭으로 통합하고, 각 항목의 버튼과 트리거 액션이 **DB 및 스크립트의 에이전트 이름과 정확히 매칭**되도록 싱크를 맞춰주세요.
    ex) 현재 개별 소스 수동 재수집은 daily에  4개만 들어있음. 근데 pipeline-log.md 보면 agent 별로 되어있음.. 일치하지 않아보임
  - 실패한 항목의 에러 메시지를 관리자 화면에서 바로 확인하고, 곧바로 [재수집 실행] 버튼을 눌러 해당 에이전트의 작업이 다시 트리거되도록 구현해 주세요.
  - 각 데이터 수집 에이전트가 적절하게 데이터 가져오는 기준에 맞게 되어있는지 확인해주세요. (개별 소스 수동 재수집 보면 Monthly에 GG_EVENTS로 된 소스가 있는데.. 이게 뭔지 왜 이벤트성 원천소스같은 이름으로 보이는데 monthly로 가져오고 있는지 등..)
  - 해당 관리자 전용 탭에서는 각 수집기들이 어디에서 어떤 데이터를 가져오고 언제 실행되었고 실행 실패는 왜 그렇게 되었는지에 대하여 상세히 나와야 하며, 각 원천소스에 대한 데이터 수집은 독립적이어야 함. (현재 gg_public의 경우, 실행 실패: GG_CULTURE_EVENTS 실패로 건너뜀이 있는데.. GG_CULTURE_EVENTS 실패로 왜 건너뛰는지 알 수 없음..)
  - 각 원천데이터 수집은 병렬 수행 혹은 독립적 수행이 되어야 하며 묶이면 안됨
 
---

## 🛠️ 요청 사항
1. **DB 스키마 정의:** 파이프라인 로그 저장을 위한 Supabase(PostgreSQL) 테이블 생성 쿼리 작성.
2. **로깅 전환 코드:** 에이전트 실행부에서 로그를 DB에 적재하는 서버 측 코드 예시.
3. **관리자 탭 UI 및 액션:** DB에서 최신 로그 데이터를 불러와 상태를 보여주고, 에이전트별 수동 재수집 API/Server Action을 호출하는 Next.js 컴포넌트 및 로직 작성.

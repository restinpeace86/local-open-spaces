- [x] **[Task 9-4-4] 설정 지역 LIKE 검색 파싱 수리 & 카테고리 탭 전역 위치 디폴트 강제 고정** 📍 (2026-08-22 완료)
  - **완료 내역**:
    1. **`get-home-feed.ts` 쿼리 파싱 정교화**: `tokensOf()`로 선택 지역을 "구체적 토큰"(분당구)/"상위 시 토큰"(성남시)으로 분리하고, `regionOrFilter()`로 `sigungu_name.ilike.%토큰%,{텍스트컬럼}.ilike.%토큰%`(events는 `venue_name`, open_spaces는 `address`) OR 필터를 적용. `fetchRegionFirstRows`를 1)구체적 토큰 ILIKE → 2)상위 시 토큰 ILIKE → 3)지역 무관 전체 조회의 3단계 캐스케이드로 개편(필요 개수 충족 시 다음 단계 생략). 클라이언트 측 우선순위 함수 `regionTier`도 정확 일치가 아니라 같은 퍼지 토큰 매칭으로 통일(`byRegionPriority`도 이를 재사용).
       - **실측 확인**: sigungu_name이 정확히 "성남시 분당구"인 행과 sigungu_name이 NULL이라도 venue_name/address에 "분당구"가 포함된 행 둘 다 이제 1순위로 매칭됨(신규 테스트로 검증). 라이브 DB 조회 결과 open_spaces는 정확 일치 854건 대비 퍼지 매칭 855건(+주소에 있지만 sigungu_name이 다른 표기인 건), "성남시" 상위 토큰까지 넓히면 1,434건까지 후보 확보 가능함을 확인. **정직한 보고**: events(당일 이벤트) 테이블은 현재 분당구/성남시로 태깅된 활성 이벤트 자체가 1건뿐이라(실측 확인), 이 쿼리 개선만으로 "당일 이벤트 0건"이 데이터 볼륨 문제까지 해결하지는 못함 — 다만 기존에도 3단계(지역 무관 전체 조회) 폴백이 있어 Hero Carousel 자체가 완전히 비지는 않았고, 이번 수정으로 sigungu_name 표기 문제로 인한 매칭 누락(진짜 버그)은 확실히 고쳐짐.
    2. **카테고리 탭(`/region`) 전역 위치 디폴트 고정**: `region-grid-view.tsx`에 지역 옵션(`districtOptions`, 실제 데이터 기반)이 준비되면 `sigunguName`("성남시 분당구")의 두 토큰 중 실제로 존재하는 쪽을 찾아 지역 드롭다운 기본값으로 자동 적용하는 effect를 추가. sigunguName은 2단 시/도(성남시 분당구)와 광역시 축약형(울산시 동구)에서 `extractDistrict()`가 뽑아내는 토큰 위치가 다르므로(전자는 도시 토큰만, 후자는 구 토큰만) 특정 위치를 추측 고정하지 않고 실제 계산된 옵션 목록에 존재하는 토큰을 찾아 사용함. 사용자가 드롭다운을 직접 조작하면(`districtManuallyChanged`) 더 이상 강제로 되돌리지 않고, 카테고리를 새로 선택하면 그 카테고리에 대해 다시 기본값을 적용함.
       - **동작 변경 안내**: 기존에는 설정 위치를 "정렬만"(다른 지역도 계속 노출) 했으나, 이번 Task 지시대로 이제는 "기본 필터"로 동작해 다른 지역은 기본적으로 숨겨진다(사용자가 드롭다운에서 '전체 지역'을 선택하면 다시 전부 보임) — 기존 Task 9-1-4 테스트를 이 새 동작에 맞게 갱신함.
  - **검증 기준 결과**: `npx tsc --noEmit`, `npm run test`(21 files/190 tests 전체 통과 — get-home-feed.test.ts에 sigungu_name NULL·venue_name 퍼지 매칭 회귀 테스트 추가, region-grid-view.test.tsx를 디폴트 필터 동작에 맞게 갱신), `npm run build` 모두 통과. `npm run dev` 기동 후 `curl /api/home/free-feed?sigungu=성남시 분당구`로 12건 전부 "성남시 분당구"로 채워짐을 실측 확인.
